# Sanitized preflight failure

```text
Built CombatSolver DLL/manifest/MemoryCleaner not found in the explicitly supplied build directory.
```

The subsequent retry used the project's default Release DLL plus the project-root manifest and MemoryCleaner output.

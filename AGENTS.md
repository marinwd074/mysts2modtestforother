# CombatSolver 项目规则

本文件只适用于当前 CombatSolver Git 仓库，不改变其他项目或全局 Codex 对话的规则。

## GitHub 同步

- 每次修改本项目代码、文档、配置或发布元数据后，完成相称验证，提交本次任务改动并推送当前分支到已配置的 GitHub 远端（通常是 `origin`），再报告完成。
- 先检查工作区，只暂存本次任务改动；保留用户已有的无关改动，不提交密钥、凭据、构建输出、缓存或项目规则排除的生成文件。
- 每个任务使用一个清晰的提交；禁止强制推送或改写历史。
- 没有 Git 仓库、GitHub 远端、认证，或推送失败时，必须明确报告，不能声称已上传，也不能擅自更换远端或上传方式。
- 用户明确要求只读或仅本地修改时，本规则对该任务让位。

## 运行证据

- 只在运行结果出现新的失败模式、形成新的验证结论，或为已有 Bug 提供新的关键证据时，才把游戏日志、Mod 使用日志、报错/异常报告和相关问题包复制到私有仓库、整理并提交推送；重复运行和等价失败不提交。
- 证据放在根目录按时间戳组织的 `runtime-evidence/` 下，与 `source/` 和 `MODS/` 分开；不覆盖旧运行，不重复提交字节相同或诊断意义等价的文件。
- 每次运行附 `metadata.md`，记录时间、游戏版本、Mod 版本、源码提交、复现上下文和文件清单。
- 上传前删除令牌、密钥、Cookie、账号标识及无关个人信息；保留堆栈、InnerException、相关日志窗口、游戏状态、动作时序和版本信息。
- 不上传完整游戏安装目录、存档、截图、崩溃转储、缓存或构建目录，除非用户明确要求；超出 GitHub 限制或无法安全脱敏时，报告问题并停止上传。

## 项目清理

- 每次项目执行后，必须清理不再需要的项目临时目录、旧问题检查副本、重复性能归档、旧构建输出和完整游戏测试副本。
- 清理前必须盘点绝对路径、用途和大小；只删除确认属于本项目且可重建的生成物，保留源码、当前发布 DLL、活动运行数据、问题包、已提交文档和运行证据。
- 不得删除正式 Steam 游戏目录、项目仓库、凭据、用户存档或正在使用的进程目录；不得对磁盘根目录、用户目录或仓库根目录执行递归删除。
- 清理完成后必须报告删除的精确路径、释放空间和文件是否可恢复；可重建的测试副本应优先删除，后续需要时再按需生成。

## 其他规则

- `source/AGENTS.md` 对源码目录提供更具体的补充规则；不能放宽本文件的 GitHub 同步和运行证据要求。

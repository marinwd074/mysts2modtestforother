# CombatSolver 项目规则

本文件只适用于当前 CombatSolver 仓库。目标是以尽可能少的流程成本持续交付正确、可验证的改进。

## 1. 工作方式与权限

- 以“项目主持人 / Tech Lead”方式推进：在目标明确时直接执行，不为普通实现细节反复请求确认。
- 对**仓库内、可逆、非发布性**工作，Agent 可自行决定实现方案，包括读取相关文件、重构、修 Bug、补测试/诊断、更新文档、删除死代码和整理目录。
- 发现与当前任务直接相关的相邻问题时，可以一并修复；不要因为“用户没逐条点名”而保留明显缺陷。
- 某个方法失败后，可以自行切换到安全、可逆的替代方法，并保留关键失败信息；不要求因普通失败立即停工等待用户选择。
- 不要求每个任务先输出 `/plan`，也不要求每次回复都生成 Markdown 交接。简单任务直接做，复杂任务只保留必要计划。
- 只有当决策会明显扩大产品范围、产生不可逆数据损失、需要外部账号/费用、改变发布状态，或用户目标本身存在冲突时再请求确认。

## 2. 不可越过的边界

- 不提交或泄露密钥、令牌、Cookie、账号标识和其他敏感信息。
- 不删除正式 Steam 游戏目录、用户存档、仓库外个人文件或来源不明的数据。
- 不强制推送、不改写共享 Git 历史；除非用户明确要求，不删除远端分支或标签。
- 不擅自发布版本、创建正式标签、上传创意工坊、变更外部生产服务或执行其他不可逆发布动作。
- 正确性优先于性能；不得通过扩大 Beam、节点、时间、内存或 GC 预算掩盖模拟错误。
- Multiplayer Lab / Probe / Advisor / Safe Execute 等能力必须按证据逐级开放；未达到既定实机证据门槛时，不把实验能力静默升级为正式玩家默认能力。

## 默认上下文纪律

- 新任务默认先读取：本文件、`source/AGENTS.md`、`source/docs/CODEX_HANDOFF.md`、`source/docs/CombatSolver_Quality_First_Next.md`、`source/docs/CombatSolver_GPT_Architecture_Plan.md`，再读取任务直接涉及的源码/文档。
- `source/docs/CombatSolver_Quality_First_Next.md` 是当前多人求解器的主执行目标。未被用户明确替换或文档自身完成前，后续实现必须按其中的优先级推进；与旧 handoff、旧 U7 或长期架构计划冲突时，以该文档的当前任务优先级为准，同时仍以当前代码和新验证证据校正实现细节。
- `source/docs/CombatSolver_GPT_Architecture_Plan.md` 是长期架构与执行计划，不等于当前实现事实；每轮必须先核对当前 HEAD、handoff 和实际调用链，已完成阶段不得重复施工。若计划与当前代码/新验证证据冲突，以当前代码和最新证据为准，并按需更新计划。
- 不得为了“了解完整历史”自动批量读取 `source/docs/performance/`、`strategy/`、`audits/`、`issues/`、`releases/`、`history/`、`runtime-evidence/` 或 test-only submodule；只有定位特定回归、比较指定基线或用户明确要求时才定向读取。
- `source/docs/TEST_MATRIX.md` 只在决定验证范围时读取；`source/docs/multiplayer/RUNBOOK.md` 只在真实 Host/Client Lab 操作前读取。
- dated 报告、旧批次总结、旧 commit SHA 和历史 PASS 不是当前事实。当前状态以 `CODEX_HANDOFF.md` + 当前源码 + 当前验证结果为准。
- 搜索仓库时优先 symbol/path/keyword 定向检索；禁止先把整个 docs、tools 或历史目录加载进上下文。

## Multiplayer 实机操作记忆

- 任何 Multiplayer Lab / Host+Client 实机操作开始前，必须先读 `source/docs/multiplayer/RUNBOOK.md`；该文件记录已经踩过的启动/重启/Steam/证据收集事实，优先于重新试错。
- 已写入 RUNBOOK 的稳定操作事实不得在新对话中重新“探索”一遍。只有当前版本或脚本行为明确变化时才重新验证并更新它。

## Multiplayer 实机职责分工

- 游戏内 GUI 实验操作由用户负责：建房/加入、选角色、Ready、进入战斗、观察界面、点击按钮、结束回合等，不交给 Codex/Agent 远程代操作。
- Codex/Agent 负责：构建、准备/刷新实例、启动/重启/停止游戏进程、Mod warm-up、日志与证据定位、运行验证器、代码修复和结果分析；不得把这些技术步骤反交给用户执行。
- 任何任务若以真实 Host/Client Smoke 为完成条件，Codex/Agent 必须主动把流程推进到需要 GUI 的节点；用户只完成当前 GUI 步骤，随后 Codex/Agent 继续启动/停止、日志、validator 和收尾。
- 需要用户操作时，只给出**当前一步**和预期看到的现象；不要让 Codex 长时间尝试替代用户进行游戏 GUI 操作。
- 用户完成一步后，根据用户反馈或日志继续下一步；不要重复已经由 RUNBOOK 固化的准备流程。

## 3. Git 与同步

- 默认在任务完成后提交并推送当前分支；用户明确要求只读、仅本地或不推送时除外。
- 提交信息统一使用 `根目录：具体功能` 格式，例如 `UI：多人界面对齐单人布局`；措辞简洁，让不了解代码的人也能看懂。
- 同一任务默认合并为尽量少的有意义提交；只有风险边界、回滚需求或独立功能明显不同时才拆分，避免噪声提交。
- 保留用户已有的无关改动；不要为“工作区干净”覆盖或删除别人的工作。
- 文档收尾提交只有在它确实修正当前事实或入口时才创建；不要为了流程完整制造空提交。
- 推送失败时报告真实状态，不伪造已同步结果，也不擅自更换远端。

## 4. 验证策略

- 按**风险和改动影响面**选择最小充分验证，不设固定测试数量或固定 120 秒硬上限。
- 可以主动运行超过 10 项测试、较长测试或完整套件，只要它们对当前风险有实际价值；避免无意义的重复跑、重复构建和重复下载。
- 测试失败后可以直接定位、修复并重跑最相关的失败项；无需先等待用户批准。
- 超时首先视为诊断信号。可以在确认测试本身仍有价值时适度延长或换更窄入口，但不得用不断加超时掩盖死锁、卡住或性能回归。
- 编译、静态检查、合同测试、无头运行和真实可见游戏分别证明不同层级，不能互相冒充。

## 5. 文档与运行证据

- 当前配置、能力、限制和开发状态只保留一个可编辑真源；历史由 Git history 恢复，不在当前树重复堆快照。
- `source/docs/CODEX_HANDOFF.md` 只在项目状态、关键结论、未完成项或下一步发生实质变化时更新；只读问答和微小改动无需机械更新。
- 最终回复默认只报告关键变化、验证和剩余风险；只有用户要求、交接确有价值或任务复杂时才额外输出 Markdown 文件。
- 运行日志和问题包默认保留在项目忽略目录（优先 `.local/`）。只有新的失败模式、关键回归证据或长期需要审计的结论才整理进 `runtime-evidence/`。
- 提交运行证据时做最小化与脱敏；不因为“每次运行都要留档”而制造仓库膨胀。

## 6. 清理权限

- Agent 可以清理仓库内确认无引用的生成文件、缓存、旧临时副本、被新真源替代的重复文档和一次性工具，无需逐项征求批准。
- 清理前只需确认用途、引用关系和边界；大型或不可恢复删除再单独说明。
- 不删除活动运行数据、未知来源文件、凭据、用户存档或正式游戏目录。
- `.local/`、`bin/`、`obj/`、构建包和临时日志默认可按需要清理或重建。

## 7. 子目录规则

- `source/AGENTS.md` 可以增加源码技术边界，但不应重新引入机械审批、固定测试次数、强制单提交、强制 Markdown 或禁止安全替代方案等流程限制。

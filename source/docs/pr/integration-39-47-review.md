# PR #39、#40、#41、#42、#44、#45、#46、#47 集成审查

2026-09-06。按用户要求进行定向审查并合并八个 PR；不提升版本或发布二进制。

## 审查与合并处理

- #39/#44：保留 Fork、监听器和热路径优化；重叠 StateStore 使用直接状态值与惰性字典，快照临时列表统一使用运行级 lease，保留异常清理和并发提交原序。
- #44/#45：回收完成与取消逻辑使用 #45，实现中保留 #44 的暂停观测、触发原因、Smart 层预测和容器复用。Windows/Linux 启动器同时保留主线检查点回放与专用停止实例入口，保留 Linux 暖进程预约修复。
- #40/#41/#47：统一路线治疗、战后回血与保命遗物消耗计价。复活回血从普通治疗收益中扣除，再计算遗物代价；搜索中展示、保留、主结果下界和最终排序使用同一口径。带骨肉继续只显示、不进入排序。
- #42：检查继承的战斗 Hook，保留未知 gameplay subscriber 的拒绝边界。
- #46：起手打击/防御权重限定为消耗、转变，保留原弃牌排序。
- 测试适配：历史卡牌快照改为比较 Original 身份；为新增事务检查补齐测试上下文和循环测试快照；GC helper 测试接入默认 GC 回退回调。没有给生产逻辑加入空值兜底。

## 本轮证据

| 验证 | 结果 |
| --- | --- |
| Release 构建 | 通过，零警告、零错误，CopyModOnBuild=false |
| Windows 结构门禁 | 通过，search_files=70 |
| StateStore、ExpansionBatch、SnapshotListBuffer、WavePolicy | 通过；后两类容器各 7 项，wave 含 10000 个有界案例 |
| GC 政策 | 19 项通过 |
| Windows headless helper、专用 StopOnly | 通过；未启动额外游戏请求、外来进程保留 |
| Linux 五个改动入口 Bash 语法 | 通过；未运行原生 Linux 游戏 |
| PredictionFailureBoundaries | 通过，runId `645460e0c01f4537be6f14d18ddfc3b2` 的 completedChecks；该请求后续搜索夹具不适用，见下文 |
| ControllerSessionLifecycle / ForkBoundaries / CombatRootSnapshot | 通过，`2953afdaf4134d0fa56706928188e5a7`；覆盖组合计价、历史、挂起事务、回手顺序与分支隔离 |
| DOP1/DOP2 / 捕获政策 / 节点预算释放 | 通过，`ddc0c139707840e59b6a4aaa4e90878d`；固定 250 节点预算，实际并发至少 2 |
| 回合结束电球击杀 / 增量回放 | 通过，`fa36fbfbcdb241bdaeac84562471a85b`，终局为 T1 |
| 长周期增长伤害 | 通过，`e3afdc58a459444fbf9e57256f78f5bb`，892 动作、445 次跨洗牌、T1 胜利、零损；不作性能 A/B 结论 |
| 必须使用保命遗物的完整自动部署 | 通过，`9cae9869cb3043f9a5db0ae2a41a5723`，复活 40 HP、T2 胜利、战损 1、零计划外重算，Instant / 0 秒，启用增量回放 |
| 永世沙漏两步原版严格差分 | 通过，`8c77f5537bf34f26b44e19afbe686df4`；普通牌 preview 保持只读、状态牌升级及兄弟 Fork 隔离 |

失败记录保留：首轮构建暴露新快照参数和静态下界上下文缺失，已修正；失败边界测试仍传 null 上下文、循环租约测试仍传 null 快照，分别在 `1bf9f56d02f44667b11344ab9c47b2ca`、`18e9ed8995df40c9aa8cb85ede6d2049` 报错后修正。`645460e0c01f4537be6f14d18ddfc3b2` 的 1 HP 敌人只展开 3 节点就获胜，无法验证节点预算释放；后续使用 1000 HP 敌人的固定工作量夹具通过。原版效果差分的第一次命令遗漏普通牌输入，`40a88f27e589471bab56fb7fd8a5e649` 在夹具前置条件处失败。

## 验证范围

原 PR 的历史性能数字与战损对照不作为本轮实测。#45 作者披露过部分旧样本战损增加 2 至 5 HP，本轮没有宣称全场景质量不退化。没有验证可见 Steam 性能、UI 外观或完整第三方 BaseLib CardModifier 栈；没有运行完整发布门禁。

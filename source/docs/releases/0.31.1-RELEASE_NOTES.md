# Combat Solver 0.31.1

- 新增战斗路线记录：首次算出的路线自动保存在本地。尝试其他打法后 SL 回到相同战斗状态，可以直接恢复原路线。
- 路线记录可跨游戏重启保留，也支持回合开始选牌。不依赖“保存并退出”按钮，快速 SL 恢复到相同状态时同样可以命中。
- 恢复路线后仍可直接执行或开启全自动；面板显示“路线已恢复”。
- 战斗状态、求解策略或游戏/Mod 构建发生变化时重新计算；手动点击“重新计算”仍会启动新搜索。

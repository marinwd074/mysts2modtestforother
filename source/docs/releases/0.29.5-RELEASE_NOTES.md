# Combat Solver v0.29.5 本地扩展更新日志

- 修复游戏运行期间 Steam 更新工坊 Mod 后，战前 worker 因磁盘版本和主进程已加载版本不同而报“隔离进程 Mod 集合不一致”。
- Combat Solver 在主进程初始化时为本次会话钉住已启用 Mod 的文件；worker 始终以主进程实际载入的版本为准。
- 保留严格 Mod 集合校验；真正的缺失、额外或版本不一致仍会中止预测，避免发布来自错误运行环境的战损。
- 快照使用硬链接，不复制大型 Mod 资源；worker 的静音、复用、内存状态与保活设置保持不变。
- 增加原子工坊更新回归，确认源目录被替换后，worker 快照仍保留旧内容。
- 十 Mod 端到端回归通过：确定预测、假设样本、worker 复用、内存状态、静音、全部保活期限、自动关闭和主进程状态不变均已验证（runId `8382cbd40ce74a44ac2cac7e444f6403`）。

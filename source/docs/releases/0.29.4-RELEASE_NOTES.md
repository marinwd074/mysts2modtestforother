# Combat Solver v0.29.4 本地扩展更新日志

- 本地扩展已重放到作者 `v0.29.1`（`f63c57c`），保留作者的回合开始顺序、动态变量、Spite、根监听器与药水槽漂移修复。
- 战前 API 升至 v5，worker 空闲期限可以在创建前或待命期间调整。
- 默认仍空闲两分钟关闭；伴生 Mod 可选 2、10、30 分钟，也可一直维持到手动关闭或主游戏退出。
- 修改待命 worker 的期限会取消旧计时并按新期限重新计时；“一直维持”会取消自动空闲关闭。
- worker 状态明确以可空期限区分有限保活和一直维持，战斗结果、缓存键、隔离静音和自动关闭语义不变。
- 四 Mod API 回归 `PRECOMBAT-API-V5-UPSTREAM-0291-013` 通过，runId `e2236a4ec85041dc95c0b3417ed0b688`。

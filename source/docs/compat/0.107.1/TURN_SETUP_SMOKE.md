# 0.107.1 turn-setup smoke

`COMPAT1071_TURN_SETUP` passed at source commit `d820a6c`'s parent
`7a73732`:

`turn=2; surface=True; selected=True; continuation=true`

The run installed the `ToolsOfTheTradePower` fixture, ended turn 1, observed
the native turn-2 hand surface, drove the planned choice, and returned to the
play phase. This is the direct runtime evidence for the 0.107.1 coordinator
signature adaptation.

Evidence: [`20260918-compatibility-turn-setup-d`](../../../../runtime-evidence/20260918-compatibility-turn-setup-d/).


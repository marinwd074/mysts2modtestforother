# 2026-09-08 计划外重算：高频共性修复

[返回问题目录](README.md)

## 材料与统计口径

输入 `combatsolver-reports-20260908-114250.zip`：188 份报告 ZIP，按 forensic session 去重为 179 场，9 份是重复导出。优先读取每场日志可见的第一条 `state_mismatch`；日志切片缺失时采用 audit 最后差异，保留这一证据限制。计数单位是独立战斗，不是重复导出数或重算次数。

录制端 environment 声明版本：0.33.6 共 21 份、0.33.5 共 67 份、0.33.4 共 30 份、0.33.3 共 14 份、0.33.2 共 39 份、0.33.0 共 9 份、0.32.0 共 7 份、0.31.0 共 1 份。基线源码为 `32db9d3`（0.33.6）。包内没有可用于本轮构建鉴定的 DLL/source commit。

原始材料与逐场分类保存在 `.local/issue-bundles/20260908-114250/`；`triage/classified.csv` 提供版本、session、分组、处理决定、原始 ZIP 路径和可见差异。材料未进入源码提交。

## 本批修复

| 共性 | 独立场数 | 代表 | 根因与修改 |
|---|---:|---|---|
| 普通能力与多实例能力的获得顺序 | 98 | `b8d41d5f…`，AEONGLASS，0.33.6 | 普通能力和多实例能力分两轮插入监听表，多实例能力被统一放到最后。改为按实际获得顺序登记逐实例引用，Fork 使用同一映射上下文；数量叠加保留位置，移除后重获排到新位置。覆盖玩家多实例能力，以及敌方 Swipe。 |
| 新召唤友方的监听顺序 | 5 | `39132dc8…`，TEST_SUBJECT，0.33.5 | 新召唤奥斯蒂追加到列表尾部，敌方监听段却插入旧边界，最终敌人在奥斯蒂前面。新增友方段同时推进敌方插入边界，恢复原版友方在前的顺序。 |
| 能力移除后重获的回合开始状态 | 3 | `b8e6592d…`，AEONGLASS，0.33.5 | 移除的能力仍留在类型索引中；再次获得直接复用旧实例，下一回合抽牌能力出现 `3/3`、`3/5` 对 `3/0`。重新获得时按原版建立新实例，重新初始化回合状态和内部数据。 |

98/5/3 是按报告症状匹配的范围，三个根因各有最小原生差分失败基线和修复证据。没有逐包重新部署，不能据此宣称 106 场整场零重算。

## 本批保留项

- 13 场 0.33.2 报告属于敌方召唤后 Illusion/Minion/Hatch 能力的监听顺序，已有 `a245573`（0.33.3）修复；本轮用既有召唤/死亡边界夹具检查相邻路径。
- 16 场落在每类只有 1–2 场的初步分组：灾厄数值、能量、球顺序、AI 历史、玩家格挡、孵化行动、历史计数、遗物计数、能力缺失和金币。本批按用户要求保留；未声称这些字段内已逐项定位同一根因。
- 44 场待进一步归因：包括 18 场手牌/弃牌字段、17 场玩家或敌方 HP、3 场敌方格挡、3 场其他能力差异、3 场执行漂移。相同字段可能来自不同效果、版本和操作，当前证据不足以把它们合并成一个高频 bug。本批没有按字段直接修改语义。

## 验证范围

三项最小严格差分与相关回归的 runId、命令见 [测试矩阵](../TEST_MATRIX.md)。比较完整 `MoveStateSnapshot` 与 `ContinuationStamp`，包含有序能力、牌堆、RNG 和 Fork；不靠排序或忽略字段消除差异。

外层 Preflight 因 `archive_size_limit:142767665` 拒绝；拆分后的 `b8d41d5f…` 代表包 Preflight 为 `materials_valid`，`restorationVerified=false`。本轮未执行原包 RestoreOnly、整场自动部署或可见 Steam 验收。纯差分没有启动正式搜索，没有开启增量搜索验证。

# 2026-09-07 三个汇总日志包：逻辑问题分诊与修复

## 范围与证据

分支：`fix/report-logic-bugs-20260907`，起点 `acf2217`（0.32.0）。先汇总，再按根因复现与修复。

| 下载包 | 报告数 |
|---|---:|
| `combatsolver-reports-20260907-135612.zip` | 9 |
| `combatsolver-reports-20260907-135521.zip` | 45 |
| `combatsolver-reports-20260907-135438.zip` | 63 |
| 合计 | 117 |

原包位于 `D:/Download/Edgedownload`。117 个报告 ID 均不同；这不代表有 117 个根因。安全检查后将嵌套包及定向提取的元数据保存在 `.local/issue-bundles/reports-20260907/raw/`，完整路径/大小/压缩量在 `triage/inventory.json`，逐报告信号与分组在 `triage/classified.json`。原始日志、联系方式与存档不进入源码提交。

按包内 environment 声明的版本统计：0.31.3 共 15，0.31.2 共 40，0.31.1 共 19，0.31.0 共 26，0.30.0 共 16，0.29.1 共 1。它们不是当前 0.32.0 的已验证失败，须检查当前调用链并运行最小复现。

## 初步分类

以下按每份报告的主要异常互斥分组；字段相同只是排查入口，尚未证明共同根因。所有组初始状态为待定位、待验证。

| 组 | 数量 | 代表报告 ID 前缀 | 直接证据 / 下一步 |
|---|---:|---|---|
| 回合准备计划差异 | 9 | `19033790`, `1aec4ced` | 手牌或弃牌顺序不同，多数数量相等；核对开局原生选择与排序时序 |
| 抽牌同步递归达到 100 层 | 1 | `663dfe38` | 核对重入抽牌、自动打出与终止条件 |
| 请求结束回合未结算即 Fork | 3 | `58ece4ef`, `2c5ff5c4` | 核对动作完成与安全分叉边界 |
| 落选分支恢复不等价 | 28 | `95e19fb8`, `60c07fba`, `44a62cbc` | 多数蟹皇相同指纹却不同评分，另有真正指纹差异；分开核对 |
| 第三方卡缺计算变量规格 | 1 | `4dcb76b4` | `MYCHARACTERMOD-INSPIRED_PURSUIT_RECORD`，按外部适配缺口单列 |
| 必需保留分支超过 Beam 容量 | 1 | `30537746` | 核对配额/去重/必保仲裁，不能直接扩大预算 |
| 终局锁定后继续回放 | 4 | `c83eef78`, `5379696a` | 核对终局边界与动作链 |
| 回放找不到逐实例手牌 | 2 | `c3f8cf86`, `84cca2ef` | 忧郁/步法含附魔或词条，核对费用与实例状态 |
| 帕尔军团出牌事务中 Fork | 1 | `39eb2b0c` | 核对悬挂选择与重放边界 |
| 临时循环出口 observation 越界 | 2 | `5ffe912c`, `cc86335f` | 核对出口观测生命周期 |
| 遗物标注回放不等价 | 1 | `8c9131fa` | 同路线敌 HP 不同，核对根与回放语义 |
| CardPlay 缺少目标 | 1 | `bc23d7c8` | 核对自动打出/重复攻击目标生命周期 |
| 续用玩家 HP 差异 | 5 | `d260341f`, `285b9283` | 回合末伤害/回血时序 |
| 续用 Power 差异 | 2 | `ab2d2a10`, `a02f6edd` | 强壮层数预测为实机两倍 |
| 续用怪物行动差异 | 5 | `e86713d6`, `90d0e56c`, `979c5109` | 双尾鼠、尸蛞蝓、巨型蜈蚣 AI / 死亡复活 |
| 续用手牌差异 | 17 | `3b1a20df`, `3303ca36`, `ba819b25` | 包括少抽、牌序、BOUND 消失、GALVANIZED 丢失，须继续拆分 |
| 执行漂移 | 6 | `4cc34572`, `100f50b1`, `92765a29` | 原始重算字段为空，须查执行时间线 |
| 续用 Y 计数差异 | 2 | `9822a702`, `033a3284` | `0/1 -> 0/2`、`1/1 -> 1/2`，确认字段含义 |
| 续用敌 HP 差异 | 16 | `dd692dd7`, `741eb26e`, `74dc017b` | 含分体复活 25 HP、1 点伤害及多次伤害差异，须继续拆分 |
| 续用弃牌顺序差异 | 4 | `37f4671d`, `45829e3e` | 相同数量不同顺序，核对回合末弃牌与自动打出 |
| 续用抽牌堆差异 | 1 | `111afbb1` | 连续反弹卡牌多一张，核对返回牌堆语义 |
| 续用球序差异 | 3 | `7dd15eaa`, `bcd73dc7`, `3b938632` | 玻璃球与闪电球相对顺序不同 |
| 续用怪物身份差异 | 1 | `387a2e1c` | 女王敌人 combat_id 不同 |
| 续用最大生命差异 | 1 | `9191ea89` | 预测 67 / 实机 69 |

## 验证记录

- `95e19fb8` 使用仓库 Preflight：`materials_valid`，schema v2，原生事件恢复材料完整，当前政策可读取；`restorationVerified=false`。这只是材料验证，尚未宣称恢复或回放通过。
- 目标为逐根因修复硬逻辑错误；纯搜索质量、玩家手操偏离与外部适配缺失分开记录，不把减少报错或放宽一致性比较当成修复。

## 修复进度

### 双尾鼠召唤：下一回合漏准备新个体行动

- `e86713d6`、`cf37ab2c`、`e7836f1b` 均出现新个体预测 AI 日志为空、原生已有首次行动，且预测 monster_ai RNG 比原生少 1 次。召唤发生在敌方回合时，初次选行动应推迟至下一玩家回合准备，但仍必须执行。
- 当前 `PrepareMonsterMovesForNextRound` 只遍历 performedMoves，漏掉本轮加入阵容但没有执行行动的新个体。原版 CombatManager 遍历当前 Enemies，按其顺序调用 PrepareForNextTurn。改为遍历当前阵容，继续传入各个体实际执行过的行动（新个体为空），复用初次 Roll、眩晕及复活规则。
- 最小 `RAT-SUMMON-NEXT-INTENT` 让 3 只原始个体中两只 SCREECH、最后一只 CALL_FOR_BACKUP，并推进到下一玩家 Play。基线 `6d34dbb147f143d58baaa535d3c4c997` 首差异为新个体 SCRATCH / DISEASE_BITE；修复后 `370dae413cb94195b2f5143a17132ad9` 完整原生状态、阵容 4 个、AI、RNG 与 Fork 全部通过。新个体位于原始个体之前的空槽，也覆盖下一行动准备顺序。
- 相邻复活边界 `d74dcb906e9543d8b1e336456de303dc` Passed，覆盖重新接合、再次死亡、直接/Fork/重新捕获根及清理前原生状态。未声称 `979c5109` 的具体死亡时间线已被恢复验证；尸蛞蝓报告仍另查。

### 其他首回合选择：基础流程已核对，原报告待定位

- `1aec4ced`、`7899d83b`、`bebb1bed` 的原生选择来源均为 TOASTY_MITTENS，不是筹码。`1aec4ced` 明确记录从 9 张手牌自动选择 PHANTOM_BLADES 后剩 8 张，首差异为 LEG_SWEEP / DEFEND_SILENT；`7899d83b` 只有顶层 godot.log 可用，含多次跑局的同来源选择。不能套用筹码弃牌重抽的归因。
- 当前 `TOASTY-MITTENS-NINE-CARD-SETUP` 使用 SILENT 初始牌组、BAG_OF_PREPARATION 与 TOASTY_MITTENS，实际开局选牌及精确状态激活通过，run `8cdd213e82be4468aee65b200ffa9fe9`。它验证基本“起始扩充手牌、消耗一张”流程，不覆盖原包全部牌组/遗物组合；本项没有生产代码改动。
- `06b250ca` 是 SimpleGrid 从 5 个候选中选择 1 张，实际生成 SERPENT_FORM、预测 ACCURACY。保留日志没有自动接管/Selected 记录，来源字段也为空；可能涉及玩家自行选择，证据不足，暂不标记模拟缺陷或已解决。

### FUNERARY_MASK：首回合随机插入灵魂遗漏

- `cb0b0936` 携带筹码与 FUNERARY_MASK，已记录的选择是 CLUMSY、FOLLY、DEFY，没有狡猾牌，也没有重算覆盖交错。手牌首差异为预测 ENFEEBLING_TOUCH / 原生 SOUL；真实状态在初始 28 张牌之外多出 3 张灵魂。检查发现 `PrepareRelicsBeforeHandDraw` 缺少 FUNERARY_MASK，而原版在玩家第 1 回合的 BeforeHandDraw 逐张随机插入灵魂。
- 最小原生 Hook 基线 `2cea9bef9f8d4b9198d1d3bf5859d694`：预测抽牌堆 4 张，实际 7 张，第一张实际为 SOUL。补齐既有遗物阶段中的生成操作，沿用逐张生成和随机插入 API，仅在 turn == 1 触发。
- `f9fe9225531b4729930707d0688e344c` 完整原生状态、牌序、RNG 与 Fork 通过；用原生 IncrementTurnNumber 将玩家回合改成 2 后单独再次调用 Hook，确认不重复生成。这是两个回合条件的 Hook 对照，不是完整两回合推进。
- `a7c54bdfd1e44721b30b184a0a8556b9` 验证真实开局同时持有筹码与面具：搜索、原生选择及准备阶段精确状态激活通过。未恢复原包完整跑局。

### 回合准备重算与接管竞态：固定执行计划的来源

- `19033790` 的原生日志明确记录：手动重算 START 后，玩家 TAKEOVER，筹码 Selected 消费旧计划，随后重算 RESULT 发布，再发生手牌状态 MISMATCH。旧计划执行时，`active.Result` 被新结果替换，执行选牌与结束后的校验来源不同。该包还选中了狡猾连续反弹，因此不把所有差异单独归因到竞态。
- 当前代码保留这一入口。最小 `TURN-SETUP-REFRESH-TAKEOVER` 在真实筹码页面生成计划、请求重算，再于重算期间接管。基线 `34ed1aaed32a4deca433ce2847c43ff7` 明确失败于“接管开始驱动旧计划”，无需依赖新旧搜索偶然选出不同卡牌。
- Runtime 在手动重算开始时清除旧 Result，使该期间的接管等待新结果；已经开始驱动选择时，排队的重算停止启动，包括等待原生页面锁期间发生接管的情况。复用现有 ActivePlan、请求队列和发布流程，没有放宽 continuation 比较。
- 修复后竞态流程 `94d006288b364d529ae5a579c5625855` 与正常刷新后接管 `4fc6aa624a5a46b1b061cb7ec35e362a` 均通过，包含原生选牌顺序与准备阶段结果激活。测试读取的 LastTurnSetupResult 仅在 ActivateTurnSetupResult 的 live/expected 精确比较通过后发布。
- 本组 9 份中有 5 份携带筹码；其他报告没有同样的重算交错证据，不能整组标记解决。`1965602a` 的保留日志被外部错误刷屏，未保留可用的选牌时序，不调查该外部 Mod。

### 卡牌增能命令：补齐禁止回能钩子

- 沿放血的既有命令遗漏检查 `CardOnPlaySupport`，另发现 11 张原版增能牌仍直接修改 `SimPlayerCombatState.Energy`。原版均调用 `PlayerCmd.GainEnergy`，因此既需要终局门，也需要 `ModifyEnergyGain`；`NoEnergyGainPower` 会把获得量改成 0。
- `CARD-ENERGY-GAIN-COMMAND` 在 10 能量、10 星能及禁止回能状态下逐牌原生差分。基线 `e8b64cd4c1e44bc598e618f581c7273d` 首张 ALIGNMENT 预测 12 / 原生 10。改用已有 `simulator.GainEnergy` 后，`0647bc3be5824b7691859854e975b80a` 的 ALIGNMENT、BORROWED_TIME、DOUBLE_ENERGY、FORGOTTEN_RITUAL、FUEL、LUMINESCE、PRODUCTION、SUPERCRITICAL、TACTICIAN、WISP、TURBO 全部完整状态差分通过。
- 本项为排查中发现的独立命令语义缺口，不是 `44a62cbc` 的已证实根因。该实验体报告还含较早的终局后回放失败，T3 原生动作与后台求解交错、之后发生 RESPAWN；最新根为 T9、实验体持有 NEMESIS。日志未给出失败落选路线逐动作状态，仍需定位。

### 落选分支：27 份蟹皇评分差异与 1 份实验体状态差异

- 逐份读取本组 28 份报告的主要错误信号和首个搜索根：27 份均为 `KAISER_CRAB_BOSS`、玩家有 `SURROUNDED_POWER`，主要信号中的 expected/actual 指纹相同；另 1 份 `44a62cbc` 为 `TEST_SUBJECT_BOSS`，指纹本身不同，继续独立定位。
- 蟹皇报告为 `60c07fba`、`95e19fb8`、`9b3acbf0`、`39bbb004`、`32336b3c`、`0b623a54`、`a950c312`、`01d4cf47`、`4fc19311`、`327c1f25`、`c61f9db4`、`b4d73301`、`2c65b641`、`6989c41d`、`de8c06b2`、`3737f392`、`c03d2015`、`05573d71`、`7ca4c4b9`、`bba92a84`、`9dbdb801`、`5b487538`、`980615a9`、`fb4c9304`、`d0fac3b1`、`2010dea7`、`52f7a8e8`。部分日志评分差 150000，即现行生命权重 30000 的 5 点，另有生死评分翻转。
- 该组合与本批已修复的包围朝向指纹遗漏一致：正式 `Snapshot` 用 `(StateKey, roundIndex)` 缓存敌方威胁，而背击取决于朝向。新增评分缓存检查：Crusher 预置 THRASH、Rocket 预置 CHARGE_UP，依次计算左右朝向、独立计算右朝向、再次命中左朝向缓存；要求左右指纹/预估 HP/评分不同，右朝向与独立计算一致，左朝向缓存值稳定。`99bb428e0c68471ba8a4c4b0d75d1234` 通过，未修改生产逻辑。
- 本节是归因线索与正式评分缓存的定向验证，不声称 27 份旧包均已恢复回放。手牌实例两份旧包均未提供 `PLAN_REPLAY_STATE` 逐动作日志，当前无法从日志重建失败路线前缀。

### 回放手牌身份：拆分费用差异与牌堆差异

- `c3f8cf86` 最终路线回放在 T3、action_index 11 要求 `MELANCHOLY+1`、费用 2、`SWIFT:2`、`BOUND:3`；回放手中仍有相同附魔与苦难的忧郁，但费用为 3。缺失的是预期语义实例，不是整张同名卡。原版忧郁在生物死亡且未阻止移除时，每次降低本场费用 1；应继续核对前序动作的死亡历史和路线恢复，不能仅放宽手牌匹配。
- 最小 `MELANCHOLY-OSTY-DEATH` 在手牌、抽牌、弃牌与消耗堆各放入升级忧郁（SWIFT 2 / BOUND 3），直接伤害击杀奥斯提。原生完整差分、直接模拟与死亡前 Fork、父分支隔离及死亡后 Fork 均通过，run `05b52a6c87034108996792f3e47ccbd6`。本项没有生产逻辑改动，也没有重现原包最终路线回放；原报告仍待查。
- `84cca2ef` 在 T2、action_index 8 要求升级步法（HEXED 2），回放手中只剩进阶之灾、防御和蜃景，同名牌已不在手牌。这是另一种牌堆或前序选择差异，不能与忧郁费用差异直接合并归因。

### 女王阵容差异：原包根因待定位

- `387a2e1c` 的 T8 `combat_id` 差异来自真实阵容已经移除随从、预测仍保留随从，并非已证明的 ID 分配错误。T7 真实行动仅有女王激怒，随后女王 HP 比预测少 9；日志没有 T7 独立根快照，不能据此断言漏算炼狱。
- 新增 `QUEEN-INFERNO-MINION-DEATH`：女王保持满血并预置 `BURN_BRIGHT_FOR_ME_MOVE`，随从 9 HP，玩家炼狱 9，打出 `BLOODLETTING`。原生与模拟完整状态、Fork 及死亡后仅剩女王的阵容检查通过，run `5e435cccefad4506a75537c2831c136f`。该具体路径当前一致；原包继续待定位，本项没有修改生产逻辑。
- 初始探针 `fc19135522d64107a21584d8d416dab3` 把两个敌人都设为 9 HP，导致整场终局，首差异为玩家 HP 77/80；它在战后取样，不能直接比较战斗内状态。改用原生清理前观察者后 HP 一致，并发现下述能量命令缺口。

### 放血触发炼狱清场：修复终局后的能量增加

- `QUEEN-INFERNO-TERMINAL` 在同一放血伤害内用炼狱击杀两个 9 HP 敌人。清理前严格差分基线 `ace01a08d43b49ecbf358cb01fc89556` 的首差异为能量预测 5、原生 3。
- 原版放血调用 `PlayerCmd.GainEnergy`，该命令在 `CombatManager.IsEnding` 时拒绝增能。补偿代码直接写 `SimPlayerCombatState`，绕过了已有命令语义。现改为 `CombatPredictionSimulator.GainEnergy`，复用终局门和能量修正 Hook，不新增状态或削弱比较。
- 修复后 `5123f0ad07074cf9926b2c554bf8dc26` 清理前完整状态与 Fork 差分通过。本项是排查中发现的独立缺口，尚不能归因到 `387a2e1c` 的非终局差异。

### 第三方改牌：按维护者要求拒绝

- `bc23d7c8` 的失败动作是 T3 手动打出 `REFRACT`，目标为空；包内卡牌为 1 费、消耗，原版规范费用字段仍为 3，环境加载 `WheelchairSpire`。维护者明确要求停止第三方调查，直接按不兼容处理。
- 根捕获增加 `WheelchairSpire` 的明确拒绝策略，使用 Mod ID 或已加载程序集名识别，沿用 `IncompatibleGameplayModException`；不改原版折射镜像、不提供第三方适配。合同 `aa1ab43130824665a2524a107521b75b` 通过；未执行第三方 Mod 或确认其每个具体补丁。
- 该报告从原版逻辑缺陷队列移到明确不兼容，后续第三方语义改写不纳入本批适配范围。

### 循环出口临时观测越界：生命周期缺口已修，原包归因待验证

- `5ffe912c`（0.31.1）与 `cc86335f`（0.31.0）均在 `TryMarkExpandedState` 发现 `PendingCycleExitObservation` 尚未完成动作准入。前者日志在约 2932 次展开后失败，不是初始卡牌解析错误。
- 当前 `MaterializeAdmittedCycleExitObservation` 会在选出最多一个出口后清空全部输入候选的临时观测。该实现来自 `3864f2ca`，已包含在 v0.31.0 与 v0.31.1 中，所以不能把这两份报告归为该提交已解决的旧版问题。
- 已验证的局部边界：串行展开和并行批次提交原来仅在父节点当前 `CycleProbeLease != null` 时调用上述动作准入；临时观测本身保存于子节点，而 `AddCyclePortfolio` 可撤销候选的循环租约。合成“观测已生成、父租约被撤销”调度元数据，沿生产准入条件取得失败基线 `4f2102226f00454396abd02b421ada79`。这尚未证明原报告实际走过这一时序。
- 准入条件改为同时检查已有子节点观测，随后仍由原 materialization 校验其有效性。修复后 `b07bd11d4dac484390a2badc67d5c8fb` 覆盖三类动作的失效清理、有效混合候选及 64 候选的单一出口签发，均通过。没有删除展开入口异常检查，也没有放宽探测预算。
- 该生命周期缺口已修；两份原报告的完整触发链仍未回放，保留待归因状态，不把局部合同写成整组验收。

### 小型怀表通道的牌序配额超额

- `30537746` 报错来自 `Prune` 的内部 `RankBest` 调用。原包持有 `POCKETWATCH` 和提供重放分配的 `VAMBRACE`，当前 Deep 开局通道会调用 6 宽排序；怀表的既有实际容量可为 7，而回合边界的牌序配额下限为 8，必保集合因此超过目标容器。
- 以四张不同格挡数值的偏折回放 24 种合法顺序及回合结束，构成真实快照候选池。基线 `ec88461ccf314096955767f71754f2d1` 复现同一异常；将牌序配额限制在当前实际容量后，`808eda35b341442081083729dd9881d1` 通过，保留 6–7 个不同身份候选，未饱和通道无丢失。原提名顺序和整体预算保持不变。
- 固定短搜索回归 `ffc089f076784745a8ccb6217b9b4221` 通过，原有零损胜利与 6 展开/15 转移一致。未重建原报告全部 Mod 或回放其 52 张牌的整场搜索。

### 弃牌重抽提前触发狡猾

- `111afbb1` 日志 854–860 行：T2 使用赌徒特酿，选择连续反弹及另外四张牌；后续预测把连续反弹留在抽牌堆，原生在弃牌堆。原版 `DiscardAndDraw` 在抽牌之后才自动打出本次弃掉的狡猾牌；药水和开局选择处理器却先调用 `Discard` 完成自动打出，再调用 `Draw`。
- 两处改用已有组合操作，不修改底层命令或增加状态。基线 `4dd54bb5380e4ebd84497e72ccdf2784` 用单张连续反弹复现错误回抽，修复后原生药水差分 `e4411503e76b49fd9d76a8c2ab9a9627` 通过。
- `GAMBLING_CHIP` 的共用选择处理器、Fork 与原生组合操作对照 `47ec379ab7f24176b8010eed8e1ac112` 通过；狡猾自动牌选择挂起边界 `3e182f6452944415b0e6f1850569e017` 通过。旧挂起断言曾要求抽牌尚未发生，已依据原版纠正。
- 测试没有重放原报告整场、五张具体选牌或全部 Mod 环境；也未以此宣称其他弃牌顺序报告同根。

### 生成能力牌遗漏 GALVANIZED

- `92765a29` 回合准备的 `AUTOMATION` 与 `ba819b25` 续用的 `MAYHEM` 均缺少原生 `GALVANIZED:8`，遭遇均为 `GLOBE_HEAD_NORMAL`。原版敌人持有 `GalvanicPower`，`AfterCardEnteredCombat` 对无苦难的能力牌直接施加；预测规范化却要求能力拥有者与玩家同阵营。
- 删除错误的阵营限制，沿用已有生成牌进入检测、词条状态及打出伤害镜像。无新增分支状态。基线 `4a50b407274a47e98c282bd5992c9522` 在当前版本明确复现生成 `AUTOMATION` 缺词条；修复后 `9ef200eadb2945e6949c1257c7987875` 的原生生成、Fork、父分支隔离与原生打出完整差分通过。
- 最小 fixture 使用原生普通难度能力层数 6；原报告层数为 8。没有重放工具箱选项 RNG、完整报告环境或全部其他执行漂移。

### 禁止抽牌与纸钱回合末顺序

- `3b1a20df`：战斗专注后存在禁止抽牌与丧仪纸钱，下一回合原生比预测多一张。预测在常规 Power 移除禁止抽牌前先结算遗物，纸钱额度消耗但抽牌被拦截。
- 最小原生差分基线 `0b8e95d32a5d452daeb31954b38de364` 复现 H[5] 缺失，预测 5 / 原生 6。修复后 `53fd32235f674b708eeb851c659bdabb` Passed，包含下一回合准备的完整状态。
- `PlayerTurnEndLifecycle.RunPhaseTwo` 统一常规 Power、遗物、晚期 Power；Search、风险预估和无人差分移除各自的阶段编排。没有增加分支状态。挂起边界 `775c8f5b3a4541609857f36daf4dd968` Passed，遗物选牌时晚期伤害尚未执行。
- 原报告全 Mod 整场未恢复；其他手牌差异仍待验证。Power 内部顺序的相邻问题见下一项。

### 禁止抽牌与黑暗之拥的 Power 顺序

- 上项沿调用链排查发现第二个独立缺口：禁止抽牌固定在全部常规 Power 之后移除，先获得它再获得黑暗之拥时仍错误阻止回合末抽牌。基线 `be6fc75970c240d293327a0fae047013` Failed，下一回合预测 5 / 原生 6。
- 移除逻辑归入常规 Power 的有序分派。正向 `7dcb893a3c034a1d9f3d704561e88438`、反向 `de4051e5c3e14b89b7bf7fc6c33ca55d` 的原生两回合完整差分通过。
- Power 指纹由无序聚合改为逐项有序语义值，续用 `P` 字段同样保留列表顺序，覆盖此前只单列的能量重置顺序。Fork/父分支隔离/不同顺序不同状态身份 `f4584f8b9805476aa9bfa83ad84f3d92` 通过；重新获得能力后的能量重置原生差分 `5cb6fd6a83004397b413dbc71db5b59d` 通过。沿用已有根/分支 Power 列表，没有新增可变状态或后台 live 读取。
- 这是由报告引出的最小可复现缺口，未据此把其他 16 份手牌报告归为同根，也没有宣称整组解决。

### 包围朝向的状态身份

- 已确认硬缺口：`SurroundedPredictionState.Facing` 影响背击倍率，却没有进入指纹或 continuation。左右方向因此可以共享威胁预测缓存和转置身份，导致相同指纹下的评分与结算不同。
- 最小失败基线 `bfa9cc597f6647929f193c9aac98f163` 明确复现指纹碰撞；修复后 `e25cc4a6d62841ed97bcc3b179e1361a` 检查指纹、续用、Fork 与 10/15 伤害通过。
- 沿用根捕获与影子状态，给 Power 身份和 continuation 增加朝向，不删除回放一致性检查。
- `95e19fb8` 的原生恢复 `4f52cb972f4447c5b87dcd4b1b3a9f2e` 因 Mod 集合不一致失败，未绕过门禁。28 份回放异常中还包括真实指纹差异，不能全部归因于本修复。

### 原生动作执行期间捕获搜索根

- `39eb2b0c` 日志 98–134 行：根捕获等待回收；玩家使用药水后打出 `LEG_SWEEP`；回收屏障打开后直接恢复捕获，手牌中已无扫腿且弃牌仍为空，随后首个 Fork 报帕尔军团事务未完成。
- `RequestSearch` 原来只判断回合阶段/操作禁用，未等待 `ActionExecutor`。新增当前原生动作及队列完成屏障，复用既有延期请求身份与生命周期取消；回合准备专用入口仍先处理。
- 最小基线 `d01150ef49014dc8ba1931a8802992e6` 证明真实动作回调内会捕获搜索根；修复后 `00f8c4d6237944dbb95061f695edc44c` 证明等待动作完成后搜索成功。未重建原报告全 Mod 环境，不将此测试称为原包整场回放。

### 药水自动打牌的强制结束请求

- `58ece4ef` 含 `DISTILLED_CHAOS` 和抽牌堆 `VOID_FORM`。此前药水路径完成后直接 Snapshot，只有手动卡牌路径消费结束请求；因此药水打出该牌后，后续候选 Fork 失败。
- 最小基线 `d470307bcc5b445f8f9e7546fb2bd577` 精确复现同一异常。卡牌和药水统一调用请求结束边界后，短搜 `bdd86b08c8ea41df963c0031fe8ccdcf` 的增量回放通过。
- `3bd9d757e90a4e8a9e90975904a1a651` 使用真实原生药水动作，对照跨回合、终局、Fork 和清理前完整状态，差分通过。

### 致命反伤后的回血与战败快照

- `c83eef78` 含狂宴与反伤怪，已用两张牌的最小夹具 `e81a73db90aa499b9481e90087989c76` 复现终局后继续展开异常。狂宴先遭致命反伤再回复 HP；原生 PendingLoss 已确定，HP 回升不撤销战败。
- 快照现在同时读取 HP 状态与已锁定的 Defeat。`4881c50d0c164d8f957bf71df61cf096` 原生终局完整状态差分通过；`1772dde4c0234433b3c0f0ef756916a8` 证明搜索正确选择先防御再狂宴的安全零损路线。

### 跨调用的死亡效果重复结算

- `ab2d2a10` 的尸蛞蝓力量为预测 8 / 实机 4。死亡入口此前只凭调用方的 `processedDeaths` 集合去重，球和卡牌外层使用不同集合时可再次触发同一死亡。
- 基线 `840bc517634f47a3815afc1e95bb4ecf` 复现力量从 4 变为 8。改用既有分支死亡阶段识别已结算死亡后，`f153493cbdb5422fa167c7cf384b9cba` 的重复通知、Fork 与原生打击完整差分通过。没有另建死亡账本。
- `61831e7dc4ec47b985ecc6f8590bf2cd` 证明蜈蚣复活及后来再次死亡的直接/Fork/重新捕获根仍与原生一致。原报告的完整球动作链尚未重放，不能把两份 Power 差异报告都标为原包验收通过。

### 重放零费攻击的历史计数

- `9822a702` 与 `033a3284` 的 `Y` 第二项分别为预测 1 / 实机 2。该项是本回合零费攻击开始次数。预测事件入口此前在 `IsFirstInSeries=false` 时整体返回，原生历史却记录每次重放；`033a3284` 的下一回合状态含回响形态和随机自动打牌。
- `0b32fe311c324ba2a83ff026aff42b26` 用带重放附魔的零费攻击复现相同字段差异。零费攻击逐次记录后，最终 `930567f5a18640c39a78c00363d84474` 的原生完整状态差分通过。
- 回响形态按出牌系列计数，保留首次执行口径，并给根历史查询补上 `IsFirstInSeries`，内部名称明确为系列数。`1c5954b23be5482497b42d66e6503899` 验证两层回响形态依次执行 3/2 次，Fork 和重新捕获根一致。未重放两份报告的完整自动打牌链。

### 能量重置后的球生成顺序

- `7dd15eaa`、`bcd73dc7`、`3b938632` 均为相邻闪电球/玻璃球顺序颠倒，实机 Power 列表中 `SPINNER_POWER` 在 `LIGHTNING_ROD_POWER` 之前。原版两个效果同属 `AfterEnergyReset`，按监听顺序结算；预测入口此前固定按类型先后执行。
- `cb45a70d288f46a6afa301c08a721ccb` 用两个 Power 复现同一球序差异。改用既有有效 Power 监听顺序，正向 `daddd03c11b54f02a99780ddabfcee0e`、反向 `7757429a90964e28b5b827e298e125c3` 的原生完整差分均通过。
- 五类受支持能量重置 Power 的有效顺序加入分支指纹及 `energy_reset_order` 续用字段；夹具覆盖顺序身份和 Fork，以及同阶段另外三种资源效果。三份原报告整场路线未重放。
- 追加边界发现根中单实例 Power 移除后重新获得仍复用旧位置。基线 `5b0b96d193af4a7ba02fc8592f7c38d3` 复现球序偏差，记录旧槽失效并按新获得位置插入后，`017caab7d1164ae0907c7e0e1b912765` 的原生重新施加及 Fork 差分通过。失效旧槽随分支复制并进入指纹。
- 满球槽激发 `a37f9fb36ac1481db00873d62a59360b` 通过，覆盖已有闪电球时生成 3 个玻璃球再生成闪电球的完整伤害、球序、Power 与 RNG。多实例 Power 的移除/重新施加身份没有在本轮扩展验证。

### 离场敌人的纸割仍触发

- `9191ea89` 来自 0.30.0，T7 开始时玩家 44/69，T8 为 42/69，预测最大生命却为 67。敌人为带纸割的咬人卷轴，玩家带反伤。原版反伤在 `BeforeDamageReceived` 触发，卷轴死后已离场，随后的攻击仍结算但普通 `AfterDamageGiven` 中已无纸割。
- 当前代码最小基线 `78d357172eef4291927b3f9772af622e` 复现同样的 2 点最大生命偏差。根监听表为死亡补偿保留已知敌人能力，却将其继续交给普通 Hook，混淆了已知数据与活动监听者。
- 普通 Hook 改用按阵容派生的活动监听视图，死亡补偿保留完整已知能力；`aaf903a158354b1c83ddbb3c097cb21d` 原生怪物行动完整差分通过。尸蛞蝓死亡奖励 `ac8a9bf19cae4502a7b3851124903b5c` 与蜈蚣复活/再次死亡 `7139a36826034e0daae2f9e36c40e6ff` 回归通过。原报告整场未重放。

### 回合末抽牌被再次束缚

- `3303ca36`、`c5cb9064`、`c2ff6e01` 的同张手牌仅预测残留 BOUND，原生没有；其中 `3303ca36` 有黑暗之拥。原版回合末清除卡牌束缚及已打牌标志，但束缚施加次数来自当前回合历史，额度不会在回合末提前恢复。
- 当前镜像在 `BeforeSideTurnEnd` 同时将施加次数归零，回合末额外抽牌因此可再次被束缚。`faa861a3c91747de952da67bcef4c82d` 复现相同手牌字段差异。
- 计数按影子玩家回合号读取和累计，结束阶段保留额度、下一回合重计；主线程捕获原生已用额度，分支新建状态不读取 live 历史。跨回合完整差分 `a8cfd38c8b74486d8dbda0763e306ff0`、已有根历史 `315a0f6fc47e466fbba6a0a349a79f92`、Fork/指纹/回合计数合同 `bcccb419227141b587959bd39455ddcc` 均通过。三份原报告整场尚未重放。

抽牌递归仍待定位：原版 `PagestormPower.AfterCardDrawn` 与当前镜像同样按虚无词条触发，原版抽牌入口也不消费移牌返回值；目前不能用“失败移牌后跳过”作为根因修复。容量/循环观测错误及其余实际结算差异继续处理。

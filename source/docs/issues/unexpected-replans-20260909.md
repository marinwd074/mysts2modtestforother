# 2026-09-09 计划外重算：0.34.4 未修复报告分诊

[返回问题目录](README.md)

## 材料与口径

日志站以 `issue=UnexpectedReplan`、`resolution=unresolved`、`modVersion=0.34.4` 查询到 55 份报告，按 `combat.sessionId` 去重为 51 场。全部报告只提取 `report.json`、异常摘要和 CombatSolver 独立战斗日志；本轮没有读取整份 `godot.log`，也没有逐包重跑。

原始包、选择清单、哈希校验结果与逐场本地分类保存在 `.local/issue-bundles/unexpected-replans-0.34.4/`，不进入源码提交。修复阶段保持报告未修复；只在 0.34.5 正式发布后标记已修复。

## 分组

| 分组 | 独立场次 | 报告数 | 当前处理 |
|---|---:|---:|---|
| 幻影之刃 / 致死性 / 不动参与的 Power 获得顺序 | 20 | 20 | 已修复并完成最小原生差分 |
| 回合开始自动出牌后的 `Y` 历史 | 7 | 9 | 已修复并完成最小跨回合原生差分 |
| 君王之剑跨回合手牌位置 | 5 | 5 | 最小路径未复现，缺少预期整手状态，保留 |
| 君王之剑临时星能费用 | 1 | 1 | 孤立问题，保留 |
| 其余低频或未归因差异 | 18 | 20 | 保留；每个初步触发点仅 1–2 场，不优先投入 |

正常手动偏离单独忽略。报告 `81201f2162284bfcb5516d795c3728cf` 既有一次 `manual_divergence`，也有后续两次独立的 Power 顺序差异；只把后两次纳入第一组。

## 已修复：历史敏感 Power 被临时移除后改变顺序

代表报告 `4b188b2e088c4826ba1b14d0252bd3bf`：第 4 回合实机顺序保持 `Strength, Afterimage, PhantomBlades, Dexterity`，预测状态变成 `Strength, Afterimage, Dexterity, PhantomBlades`。首个差异就是 Power 获得顺序；之后的续用失败是连锁结果。

根因是预测为了避开已经用完的“本回合首次”效果，在出牌前把幻影之刃、致死性或不动数量临时设为 0，出牌后再恢复。Power 生命周期把 `0 → 正数` 视为重新获得，于是预测分支把该实例移动到监听顺序末尾，实机从未移除这些 Power。现在伤害效果继续读取分支中的小刀/攻击历史，不动由格挡乘算 mirror 读取分支格挡历史；出牌入口不再改写 Power 生命周期。

失败基线 `6eba58023b214ead8829ed5effbad6ee` 在 `MIXED-POWER-ACQUISITION-ORDER` 得到 `P[1] expected Strength actual PhantomBlades`。最终 `8f5c8d96d9e342e8a2163e54e359c0d2` Passed：先制造一次攻击和格挡历史，再按报告顺序获得三种 Power，继续出牌并施加新 Power，完整比较生命、格挡、牌堆、有序 Power、ContinuationStamp、Fork 与父分支隔离。

直接证据范围：代表报告只直接核验了包内元数据与独立日志，没有整包恢复；最终通过的是从代表差异抽出的最小实机夹具。其余 19 份报告按相同首个 `P[index]` 差异、相同三种 Power 和相同临时移除/恢复路径静态归组，没有逐包重放。20 份报告 ID：

`4b188b2e088c4826ba1b14d0252bd3bf`、`baff3f1f64cb40058d79c0cd010fc1bc`、`c3cdb552a1d54ad8ba89d3000652a1b4`、`6f733c67442b4f618488924b03e73a5e`、`1f2e2fce2ec14cbeab33cf38b56df1d9`、`7e1175348df345f4ac0b9279308cd70e`、`d5890345771347ecb6c4e9a596bf6c43`、`f8ecd338197b4485bd4dd5dae88b706d`、`5ef653c8897349dc840cb1bc62f0a935`、`8e68629ec65c4a5b81faffad1e54f10d`、`681d989fbda84b889137f0d80b016eca`、`a8dc35c07c80464896048a13ccf2d23c`、`d9084f013d984a2eb7bbb67be41a9e55`、`1b1e109fdbea4db991f26c900b8fbf24`、`9419be5958224bda8f4b2e9ec0b1f1a7`、`81201f2162284bfcb5516d795c3728cf`、`c4beff8ab33b4dd89b9a9010495df2f7`、`b4c978ef34ec4c82975300e21ad9eccb`、`9037ccf3df9545149f09c4f57e23dd97`、`0a5d8947277343ec9bc251e616cc8725`。

## 已修复：抽牌自动出牌历史在阶段开始被清零

代表报告 `c68216599d1a439b972d4161a2135988`：第 2、3 回合的首个差异均为 `Y expected=0/0/0 actual=0/0/1`。独立日志显示玩家持有狂战士，回合开始抽到 Strike 并自动打出；没有选择或手动偏离介入。`167b73f525e14c7397dba954ed856da8` 是同一 session 的重复导出。

根因是无回合开始选牌时，预测先抽牌并完成狂战士自动出牌，之后才调用阶段开始处理；阶段开始入口同时承担了新回合历史归零，把刚记录的 `CardPlayStarted` 又清掉。实机的回合窗口在抽牌前已经切换，不会清除本回合事件。现在阵营回合历史在回合身份切换后、`BeforeSideTurnStart` 和抽牌前统一归零；Blur、镀层、Slow 等阶段开始效果仍留在原来的 `AfterSideTurnStart` 时点。

`HELLRAISER-TURN-START-HISTORY` 最终 `5bbb6f1081aa4206ae077b8aff733194` Passed：从第 1 回合推进到第 2 回合，抽到的 Strike 由狂战士自动打出，预测保留 1 次开始事件并与实机完整状态及 ContinuationStamp 一致。既有 `NORMALITY-AUTOPLAY` 回归 `f014ee4080bd4191805e67096eef7e8b` Passed，确认普通出牌开始计数、Fork 隔离与下一回合归零仍成立。

直接证据范围：代表报告只直接核验元数据和独立日志，未整包恢复；最终通过的是最小跨回合实机夹具。其余 8 份按相同 `Y` 首差异、同一抽牌自动出牌时序静态归组。手动加求解器 session `5079bb48a48b4904b514ca43a4defac0` 的两份报告记录的也是 `reason=state_mismatch`，不是正常的 `manual_divergence`，因此纳入；未把任何正常手动重算算入修复数。9 份报告 ID：

`c68216599d1a439b972d4161a2135988`、`167b73f525e14c7397dba954ed856da8`、`50c42b8d127146a48cc1934794e44f89`、`130a8ad1a036491eaa9d4a2695353f04`、`64bd001977434ee8a850f24cf99d488e`、`48e252dcae8d46e4a30b5940b0bc51c8`、`3c21fc10dcd1458a9383b66a05c52d53`、`e3fb071748a1462e958667762428fb7c`、`9017fe21ebb14d0b8c6c40bd8483c625`。

## 尚未处理

君王之剑位置组共 5 场：`b293142d539f4db2acc91a55d3fe939d`、`062da41d5ccb43e68193e897964f7654`、`0ae4b39658ce4b8eb0299e4fd2bfea23`、`f1ec15685c634898bcfcc55471660dc6`、`f70123edb0934eb58521dfcf3e35d5df`。共同现象是下一回合手牌首个位置差异的一侧为君王之剑，但触发动作并不一致；独立日志只保存首差异，没有保存缓存计划的完整预期手牌。最小夹具先验证单独保留跨回合，`28693119238a446fa8c6a26238602d10` Passed；再加入报告中最常见的淬炼刀刃后跨回合，`e7f09dd4f7bd4d1781a926453ab9e8ef` 仍 Passed。因此没有根据牌名猜测插入规则，也没有提交试验夹具。后续需要缓存路线的完整预期手牌，或能保留该状态的代表检查点。

`75cc48cefac8427586a71c4ab797b8d7` 是君王之剑当前星能费用 `expected=0 actual=1`，与上述牌序组不是同一状态差异，只有 1 场，本轮保留。

其余 20 份 / 18 场保持未处理。其中只有两组有重复表象：Lagavulin Matriarch 的敌方格挡 `expected=12 actual=0` 为 2 场（`38693964c78149a29afd20c5a17857b2`、`cee1db19012c47be828cb60e5470f62a`）；Exoskeletons 的 `E2.hp expected=9 actual=18` 是同一 session 重复导出的 3 份（`a0ab5a6647b64d5d9917cc963a05f812`、`5585bbcecdf8424183d2ce3e54ad0932`、`714d24b0bc48439bbd326b25214636b7`），只算 1 场。其他首差异分散在玩家/敌人生命、牌堆、Power、遗物计数、怪物行动、苦难层数和卡牌费用，每个触发点仅 1 场；本轮没有按字段硬合并。

本批最终在最新 0.34.4 的 55 份 / 51 场中修复并归组 29 份 / 27 场，保留 26 份 / 24 场。查询时其他旧 Mod 版本另有 70 份未修复报告，本轮按“优先最新版本”没有展开，不能视为已覆盖。

## 静默猎手指定接收窗口复查

2026-09-09 续查先用 `reports.py filters` 确认稳定 ID，再执行 `list --filter issue=UnexpectedReplan --filter status=unresolved --filter version=0.34.4 --filter character=SILENT --limit 100`。接口返回 `total=17`、`nextPage=null`；这 17 份的 `receivedAt` 均显式携带 `+0800`，范围为北京时间 2026-09-09 04:01:38 至 13:12:33。

按本轮指定的北京时间 `[2026-09-08 22:00:00, 2026-09-09 00:00:00)` 筛选，最终报告 ID 集合为空，session 集合为空。该结果是当前未解决视图，不能用上一批 26 份的历史总数代替，也不能将窗口外报告纳入本轮修复。

本轮直接验证、静态根因归组、原包整场回放均为 0 份；没有新增语义修复、最小失败基线或行为通过结果，没有更新日志站备注及解决状态。已有发布和测试记录保持原口径。

任务分支 `fix/silent-unexpected-replans` 从本地 `main` 的 `2c7bee5` 建立。访问远端时默认 TLS 后端及 schannel 均握手失败，因此未能确认远端最新 main；本轮只提交该筛选记录。

本轮 L0 验证：`dotnet build CombatSolver.csproj -c Release` 成功（0 警告、0 错误）；`pwsh -NoProfile -File tools/verify-refactor-boundaries.ps1` 返回 `REFACTOR_BOUNDARIES_OK search_files=78`。无行为变化，开发笔记与测试矩阵没有新增修复或行为证据条目；未启动 headless 进程。

## 修正版本范围后的静默猎手分诊

用户随后明确取消 0.34.4 限制。保持上述北京时间窗口、`character=SILENT`、`issue=UnexpectedReplan`、`status=unresolved`，跨版本 `list --limit 100` 返回 38 份、`nextPage=null`，按 `receivedAt` 筛到下表 10 份 / 10 场。其中 0.34.3 为 7 份、0.34.2 为 2 份、0.34.0 为 1 份。原始包安全解压在 `.local/issue-bundles/silent-unexpected-replans/<id>/raw/`，独立日志的 session 与元数据一致。

| 报告 ID | session | 版本 | 北京接收时间 | 分诊 |
|---|---|---|---|---|
| `480f1887e3c6436b9f0d1d6da1eaf8c5` | `72801196b5244ec18dccb2105e64c56b` | 0.34.3 | 23:36:34 | T6 幻影之刃 / 毒雾 Power 顺序 |
| `7dd2c1bd33994da1bc12c72ee2b42824` | `7825374bc7164f61b8b655a75ee03ec6` | 0.34.0 | 23:28:21 | T3 玩家与猎人杀手生命差异，待定位 |
| `f26001be77bd496894dc98a783cd2ac7` | `5143a27a445a420086a7ca6bd483a4b7` | 0.34.3 | 23:10:22 | T5 幻影之刃 / 精准 Power 顺序 |
| `5a8da9764a574969afbd75e7bf440a71` | `8ed5040a8edc4a9389b0d1313030a391` | 0.34.3 | 22:50:31 | T4、T5、T8 幻影之刃 / 精准 Power 顺序 |
| `a6aed92b4ae54c5bae8d9a235b41dad1` | `68675dfc2cd04f6ab3863e3acc2bd4a0` | 0.34.3 | 22:47:58 | T6、T8 幻影之刃 / CHAINS_OF_BINDING Power 顺序 |
| `ff078592cc174853b32dd5c787ef72fc` | `da23c56fdbd14fdda6bf4ea912cd9497` | 0.34.3 | 22:44:41 | T5 幻影之刃 / 虚弱 Power 顺序 |
| `2991ed512c4845efb8be4519ec165e20` | `eafa93b2d7774f109c8e2fa98efe9f01` | 0.34.2 | 22:43:13 | T2 女王生命差异；T6 幻影之刃 / 脆弱 Power 顺序 |
| `835b630a19a44c7c897d63dfc56d9a49` | `720e6a3e726649d2a82206dffcf0fa42` | 0.34.3 | 22:43:01 | T9 幻影之刃 / 精准 Power 顺序；T10 替补巨斧机器人生命差异 |
| `9e517f6cf0274edcbf728506d90f7735` | `a37b305e3f1d4e53a56b8b3f0c963d1a` | 0.34.3 | 22:24:39 | T9 幻影之刃 / TRACKING Power 顺序 |
| `86fb33c03df64b3eb796b8a79ee7f68f` | `e4300ead375242f28b7f02dd9181d875` | 0.34.2 | 22:07:48 | T4、T5 幻影之刃 / 力量 Power 顺序 |

### 已有修复覆盖与证明范围

9 份出现幻影之刃顺序错位，按首个 `P[index]` 差异及既有临时移除/恢复路径静态归入 `7915075`（已随 0.34.5 发布）。其中 7 份记录的重算均属于此组；`2991...`、`835b...` 各有另一条独立差异，整份仍待定位。未把这 9 份算作本轮新修复或直接回放通过。

上一批该根因的最小失败基线与最终通过证据仍见本页及测试矩阵。本轮读取了 10 份包内元数据、异常摘要和独立日志；新增修复直接验证为 0 份，完整报告按已有修复静态归组为 7 份，另 2 份仅部分信号同组，整包直接回放为 0 份。没有正常 `manual_divergence` 被计入。

已对上述完整静态归组的 7 份逐份 `get` 获取旧备注和 revision，合并写入“0.34.5 已有修复的静态归组”及旧测试证据引用，明确未逐包回放，全部保持 `resolved=false`。另外 3 份没有写成已修复。此次没有新修复，因此没有使用“分支已修复，待发布”措辞。

### 未定位项与本轮最小验证

- `835b...`：T9 结束前旧巨斧机器人剩 3 HP，被 3 点荆棘反伤击杀；T10 替补为原生 `104/104`，预测生命 `103`，独立日志未报告 RNG 差异。检查了补货生成、死亡移出阵容与生命判重调用链，但两个最小夹具均通过，不能把死亡清理时序认定为该报告根因。还需对齐原报告的旧个体最大生命、召唤前 Niche 完整状态及同一抽样候选集合。
- `7dd2...`：T2 精密计算后敌人生命两侧均为 80；准备丢弃 MIRAGE 与狡猾牌 UNTOUCHABLE，之后 ULTIMATE_STRIKE 预测造成 8 点、原生造成 9 点，T3 玩家生命预测 22 / 原生 23，敌人预测 72 / 原生 71。候选调用路径涉及嵌套自动牌与 Tender 的出牌完成补偿，缺少内层卡结算前后两侧力量、敏捷与完成事件的逐步对照；没有将两个生命差异独立修补，没有声称该根因已确定。
- `2991...`：T2 女王生命预测 350 / 原生 364，先于 T6 的 Power 顺序信号。T1 路线包含 MURDER、DAGGER_THROW 与逐目标攻击；缺少与每次实际受伤对应的完整预测敌方状态核对，未把 14 点差异归到幻影之刃顺序修复。

`STOCK-RESPAWN-HP`（直接打击击杀补货个体后推进至 T2）在未改生产源码上 Passed，runId `a32c5e203ed54ca281ef9d961c78789e`，28.12 秒。`STOCK-THORNS-RESPAWN-HP`（T1 敌方 ONE_TWO_MOVE 被荆棘击杀后进入 T2）同样 Passed，runId `d2d88c43f8f84e35a4b89f427e5654b4`，27.56 秒。两者使用完整 MoveStateSnapshot/ContinuationStamp 比较和预测 Fork 对账，只证明这些最小输入一致，没有取得本报告的失败基线。命令见测试矩阵，未运行正式搜索或扩大预算。

随后远端 fetch 成功，确认 `origin/main=2c7bee5`，此前 TLS 障碍已解除。本轮新增测试夹具与分诊记录，生产逻辑、版本与已发布日志均未修改。

## 后续修复：补货的原报告 RNG 已复现并消除差异

继续读取 `835b630a.../replay/current/checkpoints/000017-search_request_AutoTurnStart.json`：T9 旧个体为 `20/96`，Niche 为 `54:8740133261244141179:9651599535138124467:1850010101246481511:2495404468890984412`。前述通用种子未覆盖这个抽样点。

原版在 AfterDeath 内生成替补，旧个体的最大生命 96 仍参与排除；旧预测在移出阵容后才补偿生成，候选集合多了 96。因此相同随机状态给出预测 103、原生 104。已将补货生成移到 AfterDeath 镜像，删除死亡清理中的重复生成入口。最小失败 `416f94d9708746018de96cb3a37efb26` 和最终通过 `70aa5edb08e34137b61fc3d2c75d7978` 精确覆盖该首差异；相邻直接击杀路径 `403c3b881b8c45f2a4168ced84cdf54a` 也通过。

该报告直接读取证据并以等价最小两回合夹具验证，未整包恢复或回放。此前 T9 的 Power 顺序差异仍归入 0.34.5 已有修复，本次只修 T10 的补货差异。

## 后续修复：温柔重复处理精密计算内部的自动牌

`7dd2c1bd33994da1bc12c72ee2b42824` 的错误发生在 T2 精密计算弃牌并自动打出 FLICK_FLACK 时：内层出牌完成时温柔已扣属性，外层 `ApplyCardPowers` 的历史范围又包含这一完成事件，于是多扣一次。后续准备触发 UNTOUCHABLE 时少 1 点格挡，ULTIMATE_STRIKE 少 1 点伤害；T3 温柔已经按错误计数恢复属性，只剩 HP 差异可见。手动准备的单独选择发生在外层效果补偿之后，所以此前该较小对照通过，不能覆盖精密计算路径。

最小失败 `289cb0270eeb48d7b4ac81e3247c85e3` 直接得到力量预测 -3 / 原生 -2。温柔改为由 AfterCardPlayed 镜像逐次结算，删除历史范围中的温柔补偿；既有根捕获、分支计数、Fork、状态键和回合末恢复保持同一所有权。最终两回合 `94c01fe41a564329a15f54ae62d66dc4` Passed，相邻手动选择 `45bf88eef731446cbe7d11d279b2a792` Passed。该报告直接读取日志并以等价最小夹具验证，未整场回放。

## 后续修复：谋杀重复计入初始抽牌

`2991ed512c4845efb8be4519ec165e20` 独立日志 `000.jsonl` 的顺序是：先 `TURN_SETUP_ROOT_CAPTURE`，再出现赌博筹码原生选牌请求 `options=9`，最后 `TURN_SETUP_SEARCH_START phase=auto_pre_play`。预测仍从早期根模拟初始抽牌，谋杀倍率却读取此时已有 9 次初始抽牌的 live 历史，再加上分支初始抽牌及后空翻的抽牌事件。

首次 MURDER 的预测伤害记录为请求 21、力量及易伤修正后 33（女王 416→383）；同一动作原生 416→397，只造成 19。多计入 9 次抽牌恰好解释这 14 点伤害差；后续女王生命 350 / 364 是连锁结果，并非 T6 幻影之刃 Power 顺序问题。

谋杀现在读取已有根快照中的抽牌历史，加上分支自己的新增抽牌事件，Fork 共用冻结根。最小失败 `40301c95dd4b4bfba22aa1abb4e3fa96` 在一次实机抽牌后观察到旧父分支值 8→9、子分支 9→10；最终 `11ea828e2e69419fa522008201fb5e5e` 验证值保持不变，并继续真实出牌、推进到 T2，完整状态一致。该报告直接读取证据并用等价最小夹具验证，未整场回放。

本次继续修复的 3 份 / 3 场均有直接代表证据和本轮最小失败/通过结果；前次 7 份仍属于 0.34.5 已有修复的静态归组。合计 10 份中直接等价最小验证 3 份、完整静态归组 7 份、整场原包回放 0 份。所有报告仍保持 `resolved=false`，新修复等待后续发布。

本次根因提交为补货 `8a43a18`、温柔 `de47884`、谋杀 `1f65e7f`。三份报告均已逐份读取当前 revision 和旧备注，合并更新为“分支已修复，待发布”，保持 `resolved=false`，没有发生 409 或鉴权失败。

最终 Release 构建 0 警告 / 0 错误；结构门禁 `REFACTOR_BOUNDARIES_OK search_files=78`；CoverageCatalog `--verify-effective --verify-roster-sources` 通过（3035 hooks，0 未分类，0 缺实现，0 引用未通过测试，51 roster 调用源 / 0 未解决源）。最终 5 项行为验证均通过，未重复通过场景。私有进程 PID 41456 已停止，`silent-replans/game` 经仓库所有权校验清理，诊断材料保留；Steam 游戏目录、用户 ZIP 与 `outputs/` 未修改。未提升版本、打包、打标签或推送。

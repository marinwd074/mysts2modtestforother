# 女王大预算搜索的 NoGC 回退恢复（2026-09-13）

[返回性能索引](README.md) · [结构化样本](queen-gc-recovery-20260913.json) · [此前的恢复和分配优化](queen-replay-optimization-20260913.md)

## 结论与范围

本轮修复的是 NoGC 在一次内存压力/意外收集后退出、剩余搜索一直停留在普通 GC 的路径。保留现有回收完成确认和搜索提交边界，允许有界恢复；不更改玩家搜索预算、Beam、候选顺序、剪枝、评分、药水策略或默认 NoGC 配置。

实际女王根的固定工作量交错对照中，最终方案平均搜索耗时 **37.8816→34.5280秒（−8.853%）**，Gen0计数 **674→55（−91.840%）**，GC总暂停 **12.2317→6.0585秒（−50.468%）**。84项非时序/非调度结果字段、40步完整动作和其余结果文本一致。恢复后的内存预留有代价：采样峰值RSS **4.5382→5.2227GB（+15.083%）**，最大单次GC暂停均值 **1.7133→2.1653秒**，没有证明最坏暂停改善。

全部新增运行均为 Linux headless；没有启动可见 Steam，也没有 Windows 实机验证。上述比例只属于下述复现配置，不能外推成原Windows请求提速比例、FPS改善、完整战斗胜利或原始三层大预算请求已经在120秒内结束。

## 原Windows问题包的故障边界

原包 `CombatSolver-0.37.0-QUEEN_BOSS-bc494904718141aab26bf81fa1ab1a25.zip` 的本次搜索记录：

- 根为35张牌、2个Power、66个监听者；Beam512，100000节点，300000ms，DOP16，NoGC配置16GB。
- 搜索开始后46.588秒，第七次已确认后台回收完成时，物理占用29,291,520,000B，高于策略上限29,228,695,790B，因此没有再次申请NoGC，进入`system_headroom_insufficient`回退。
- 此时已展开60388节点，仍有37820个回合结束候选和1060个frontier节点；回收后的托管存活约5.042GB、堆9.535GB、碎片4.494GB、已提交27.547GB。这些不同口径不能互相替代。
- 45.657秒的帧样本记录累计GC暂停7.2511秒；236.888秒的样本为130.9225秒，展开数76609。约191秒窗口中增加了约123.67秒GC暂停，而节点只增加16221。

日志证明退出原因及后续暂停与慢搜重合，没有CLR触发栈，不能据此断言每一次后续收集的具体触发原因。回合结束候选仍参与整层注释、保路及下一回合展开，不能直接删去或提前剪枝来降低内存。

## 修复机制

`SearchGcPolicy.Recovery.cs` 属于 Runtime。Search只在`EnsureMemoryForNextCommit`已排空边界调用注入信号，仍按原序提交工作。

1. 意外退出后沿原路径结束/协调区域并确认后台Gen2完成。如果还没有在这份堆状态上尝试失败的预留，保存该完成index，允许下一个安全边界直接使用；不再为了等待第二次Gen2而让大批工作先进入普通GC。
2. 首次准入失败、或已经申请失败的预留，须等新的已完成Gen2再评估；普通观察至少间隔2秒。每scope最多3次真正预留尝试，后续尝试保留退避；新的退出事件不能清零次数或缩短退避。
3. 恢复预算至多为当前物理余量的一半，同时不超过用户配置。小于512MiB、或不足以容纳已知下一次不可分割工作时不尝试。恢复后的区域上限保留到本scope结束，避免下一次回收立即扩回原大预留。
4. 恢复使用`TryStartNoGCRegion(..., disallowFullBlockingGC: true)`，自身不调用强制GC，也不加入要求搜索退出的deferred完成链。已在途工作和已发出的回收仍须先排空。
5. 取消、退出NoGC、Dispose及新的scope代次会阻止旧探针复活。显式关闭、不支持的平台/尺寸、不可分割工作主动选择普通GC、收集确认超时均不自动恢复。

复用的是已确认回收的完成证据，不是共享可变战斗状态。只有恢复成功才清除系统压力回退的保守并行标记，原队列增减和内存预约规则不变。`fallback_latched=true`旧日志字段仍表示该检查点结束时处于普通GC；应结合后续`GC_NO_GC_RECOVERY`判断是否恢复。

本地游戏runtimeconfig声明.NET9.0.7。微软文档将`FullBlocking`定义为阻塞Gen2、`Background`定义为后台Gen2；采样代码也检查Generation字段。[GCKind定义](https://learn.microsoft.com/en-us/dotnet/api/system.gckind?view=net-9.0)。恢复API不允许为了预留而主动触发完整阻塞回收，但这并不是消除所有暂停的保证。[TryStartNoGCRegion](https://learn.microsoft.com/en-us/dotnet/api/system.gc.trystartnogcregion?view=net-9.0)。该版本SOH预留沿临时分配区扩展，因此全堆`FragmentedBytes`不构成下一轮SOH一定可复用的容量证明；新恢复预算不把它加到物理余量上。本轮没有修改旧常规区域重建的碎片估算路径。[.NET9.0.7 GC实现](https://github.com/dotnet/runtime/blob/v9.0.7/src/coreclr/gc/gc.cpp#L23959)。

## 固定工作量与全部对照

原包已通过记录的Continuation对账；旧模型编号映射缺失导致native-state不可比较的限制保持，见此前报告。A为`78dce1b`生产行为，D为本轮最终方案。均加载真实BaseLib/Ritsu、独立新进程、正常coordinator；只在搜索指标捕获后写出完整结果与动作，搜索内无额外性能探针。

为在120秒请求上限内稳定复现晚期NoGC退出，诊断配置保留Beam512、分支100/42/54、DOP16，设置固定20000节点和2GB NoGC。全局仍为Smart，但两个已有药水槽都设为Disabled，以只运行无药主搜索；这是诊断策略覆盖，不是修改用户设置，也不是整个原始Smart请求。每次实际20000展开、349198转移、212548选择，8个搜索回合层，NodeLimit，预测HP82/敌HP508，非完整胜利。

基线A的搜索已经产生完整结果；请求的Failed来自随后“配置的NoGC区域在搜索后仍保持”的断言，不能写成Passed，也不是模拟失败。D均通过该断言。Gen0计数包含更高代收集，不把Gen0/1/2三个计数相加当总次数。

### 最终交错序列：A3 → D1 → D2 → A4

| 样本 / runId | 搜索秒 | Gen0计数 | GC总暂停秒 | 最大暂停ms | 峰值RSS GB | 请求状态 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| A3 / `fb4bbcccb2334a088e10803c5f22f4fc` | 37.9534 | 675 | 11.9699 | 2059.278 | 4.8116 | Failed |
| D1 / `a7e9c0b5964144189baf0961c2cb8987` | 34.9647 | 56 | 5.9262 | 2355.663 | 5.1828 | Passed |
| D2 / `7032c5157680478a9f1b3f87119c2318` | 34.0913 | 54 | 6.1909 | 1974.869 | 5.2626 | Passed |
| A4 / `659ea7866443435aad14deef92ecc32b` | 37.8098 | 673 | 12.4935 | 1367.399 | 4.2648 | Failed |

两对最终候选均更快。累计worker分配19.5592→19.5841GB（+0.127%），本轮收益来自GC运行状态恢复，不是再一次分配削减。Gen2计数24→31，显式检查点收集11→15；普通高频收集减少，同时有更多受控的完整回收。恢复日志出现在对应回退日志后0–1ms；这只是两条日志之间的间隔，不包含此前已执行的回收耗时，也不表示整个回收只花1ms。

### 先行方案与未取得收益的尝试

B等待另一轮完成Gen2，并在下一个检查点才开始观察；C把观察起点移到回退时刻；D进一步复用原检查点已确认的回收证据。B平均搜索36.3124秒，较其A1/A2基线少5.136%，GC总暂停少16.162%；C没有建立额外提速。B/C仍在回退后约8.3–8.6秒才恢复，说明仅提前计时不足以避免这一段普通GC。D保留有界重试，但去掉了在已有有效完成证据时再等一轮Gen2的要求。

| 样本 / runId | 搜索秒 | Gen0计数 | GC总暂停秒 | 最大暂停ms | 峰值RSS GB | 请求状态 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| A1 / `122ce562b3744e5caf184a6e869c23ab` | 38.5936 | 680 | 12.5640 | 1892.251 | 4.7361 | Failed |
| B1 / `eab4343f2b7f44f1995e6d209886b1bb` | 36.3744 | 518 | 9.9859 | 2076.963 | 5.4662 | Passed |
| B2 / `5433d71357e4409c8664f92c3b75490e` | 36.2505 | 520 | 10.0929 | 1488.146 | 5.4604 | Passed |
| A2 / `14804e702a0a42b89dcf02bb1e7015fb` | 37.9630 | 678 | 11.3855 | 2366.286 | 5.0316 | Failed |
| C1 / `c49dd8c0ef174912951ce1b7d1cbea06` | 36.6794 | 519 | 10.1562 | 2133.209 | 5.3939 | Passed |
| C2 / `7867d81548794bdca1e2318a9e9f9480` | 36.6584 | 518 | 10.0673 | 1929.212 | 5.4393 | Passed |

全部十个样本的84字段/40动作/其余结果文本相等；A3/A4兼作后续实验的首尾基线，未删除漂移较大或最大暂停更差的样本。完整指标、运行状态和恢复日志摘录均在配套JSON中。

## 其余验证与边界

原Smart药水策略、16GB NoGC、固定每层20000节点的完整三层请求：基线 `9e14a4cc7bcc485e9c066c8cd7bb6cf6` 和最终 `0b5ff8b5bf54475da5075f06e42309ef` 均Passed，实际60000展开、1162247转移、717525选择；84字段、40步完整动作及其余结果文本一致。搜索64.2469→65.1257秒、分配60.3121→60.2969GB、总GC暂停5.4678→3.1512秒、峰值RSS16.6543→18.5104GB。两次均无NoGC退出，因此只是未触发恢复的质量哨兵，单次时间/GC差异不能归因于恢复机制；原100000节点配置不在此证明范围。

独立工具编译当前生产GC源码，基础20项、scope8项、检查点1项、恢复状态机6项、真实CLR恢复生命周期2项通过。生命周期合同实际建立1GB区域，由测试主动GC制造退出，穿过生产检查点后立即恢复，恢复自身一次预留且零强制收集；取消、退出请求和Dispose保护通过。工具与可重跑命令见[GC检查](../../tools/CombatSolver.GcPolicyChecks/README.md)。

组合`VerifySearchPolicySnapshot`合同未通过：女王根的最终候选 `635b265a358040ddbd84c6f5e8d81241`、修复前基线 `3a2c7ca6a3fe477faccdcbd2a783ba0b` 均原生SIGSEGV/exit139；小型Bowlbugs根加同一开关的候选 `3e3af625c4c847e493a6e2d3dd306a64` 同样崩溃，无结果文件。基线也失败，不能据此认定新恢复代码引入崩溃；但根因尚未定位，也不能把它记为通过。已保留本地转储，托管栈解析失败。

随后只运行现有`STAND-PAT-MEMORY-BOUNDARY`聚焦合同，`fee97c057e1744dea055fe53ad7da20a` Passed：真实DOP1/DOP2完整结果与动作等价，注入取消/异常后的worker排空及根复用通过；人工小区域穿过剪枝内存检查点，结果与串行相等。这项不包含额外的组合策略合同，后者继续标记未通过。Bash/PowerShell结构门禁均通过，89个Search文件。最终正常Release构建通过，0警告/错误，10.48秒；产物不含性能对照用的搜索后结果导出。

下列失败/超时是本轮实际实验，不纳入上述单主搜索性能均值：原Smart/20000节点/2GB的基线与先行恢复原型均120秒超时；全局Disabled仍会执行开局能力牌审计，因此此前两次名为primary的请求也不是单主搜索并均超时。随后通过逐槽Disabled保持全局Smart，才取得上面的固定单主搜索结果。没有放大120秒超时，也没有把不同审计工作量混成提速。

最坏单次暂停和峰值内存尚未改善；NoGC必须为低回收频率保留空间。Windows物理压力门禁、恢复上限和生命周期已纳入代码/合同，但Windows长会话及可见暂停仍需该平台的数据。原始100000节点、完整Smart药水审计和300000ms配置的端到端耗时，不由本轮单主搜索比例保证。

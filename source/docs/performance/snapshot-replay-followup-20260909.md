# 快照与重放热点复查及 PR 收口（2026-09-09）

[返回性能目录](README.md) · [结构化证据](snapshot-replay-followup-20260909.json)

本次从 `3f2ac26` 继续细分快照和动作重放成本，试验投影洗牌的显式比较委托。预定 B–C–C–B 筛查中，normal 均值只减少 **0.216%**，两次基线漂移 **3.190%**；Short 均值增加 **0.321%**。没有建立稳定的新收益，原型已撤回，按用户要求整理此前验证过的性能分支为 PR。

## 1. 子路径成本与取舍

沿用 `a85f3e7` 上的 Linux `perf cpu-clock:u` / 199Hz / JIT 调用栈：21,965 个搜索样本。该提交与 `3f2ac26` 的快照和回放实现相同；这里只复用它来选择实验，不把旧比例当作本轮提速。inclusive 区域重叠，不能相加。

| 路径 | 搜索样本数 | 进一步定位 |
|---|---:|---|
| 投影洗牌 | 904 | 排序 440（全搜索约 2.00%）；原生比较 193、动态值查询 109，均包含子调用。 |
| 状态键构建 | 1,423 | 战斗状态指纹 943，Power 指纹 192；不是一个可以整体省略的扫描。 |
| 可达手牌估值 | 964 | 可玩性 785、费用 Hook 375；前序版本已复用本次可玩性查询返回的费用。 |
| Power 污染归一化 | 593 | 根卡身份查找 237；记录生成牌首次进场，即使当前没有相关 Power 也不能随意跳过。 |

免费打牌与虚空形态的机会估值分别只有 38 / 10 个可见调用栈样本；内联可能隐藏单独帧。这不足以支撑扩大为跨快照费用缓存。继续维护现有分支所有权和第三方回调边界。

## 2. 投影洗牌原型筛查

仅将私有列表的 `Sort()` 替换为显式静态比较委托，仍调用原 `PredictedCard.CompareTo`，保留 null 比较、原 RNG 克隆和洗牌。没有新增排序键缓存。原型补丁保存在 JSON；最终代码恢复原实现。

固定 NECROBINDER / AEONGLASS_BOSS 的 38 牌、19 遗物、2 药水输入，VeryHigh / DOP8 / NoGC 配置 16,000,000,000 字节。每项新进程先做一次不计时 Short 预热，再测 Short 和正常搜索，每请求 120 秒、首结果停止；全部样本保留。正常搜索使用既有 Deep profile。

| 顺序 | Short 秒 | normal 秒 | normal 分配 GB | RSS 峰值 GB | GC 总暂停 ms | GC observed max ms |
|---|---:|---:|---:|---:|---:|---:|
| b1 | 4.4183 | 22.8441 | 29.6004 | 17.241 | 46.786 | 29.596 |
| c1 | 4.4559 | 23.1271 | 29.5971 | 15.128 | 1743.356 | 1712.572 |
| c2 | 4.4886 | 23.1898 | 29.5987 | 15.125 | 1735.930 | 1704.921 |
| b2 | 4.4976 | 23.5730 | 29.5991 | 15.113 | 1817.792 | 1782.546 |

normal 均值 **23.2085→23.1584 秒**；Short **4.4580→4.4723 秒**。normal 候选均位于两次基线之间，GC 仍出现约 1.7 秒长暂停，不能把 0.216% 写成已经建立的提速。RSS 的样本差异也不构成内存收益证明。

全部 8 个测量请求 Passed：原始 90 个 RESULT 字段一致，其中 88 项为非时序字段，另两项 `phase/deep_triggered` 是耗时派生标签；Short 的 59 条、normal 的 64 条完整 ACTION 逐条一致。Short 为 10,000 展开 / 144,368 转移 / 101,808 选择；normal 为 46,239 / 636,428 / 431,140。没有执行原型的专门原生洗牌合同，也没有保留未经该合同验证的排序改动。

## 3. 合并最新上游

PR 前合并 `upstream/main` 的 `7ac005e`，保留其诊断日志、双语界面、凡庸开始次数及迟缓 Fork 修复。三处冲突是开发笔记、测试矩阵和并行度提示；前两处保留双方内容，提示沿用本分支 8/4/2/1 默认规则，并接入 `SolverText.Get`、同步英文资源。没有另行改版本或打包。

上游新增的开始次数进入状态指纹，原来 `3f2ac26` 的目标工作量不再是合并版的数值基线。初次合并验证已观察到主搜索 10,000 展开 / 144,276 转移 / 101,420 选择，预测战损 4、使用两瓶药水；最新上游的同一输入也得到这些值。不能把旧分支的性能倍率直接标到这个合并版本上。

合并构建成功（0 警告 / 0 错误），Bash 与 PowerShell 结构门禁均为 `REFACTOR_BOUNDARIES_OK search_files=84`。并行度提示通过中英文键、全部 CPU/DOP 档位和占位符 L0 检查。

| 合并边界验证 | runId | 结果 |
|---|---|---|
| merged-policy | `4286e80ab6b145ca8e778c03ddb3a7e5` | Passed，57.41 秒 |
| merged-normality | `4c8a33fd786144e5aa57f6d56185d646` | Passed，5.77 秒 |
| merged-slow | `f876d91c61a04a54aeaa7cb7a2f89157` | Passed，3.91 秒 |
| merged-short | `3404f8a1ddab413d944b6c93fb3e749c` | Passed，24.21 秒 |
| merged-sentinel | `5fe36f290f8147ddbb4b756a521876b5` | Passed，4.86 秒 |

并行合同实际执行双 lane 的 257 槽位、取消/失败排空、复用和 235,536 字节分配记账；Deep 1,000 节点 DOP1/2 合同在新指纹下执行 587 次待命探针。凡庸合同比较重复打牌开始次数、完整原生状态、Fork 隔离与回合清零；迟缓合同覆盖回合重置后的 Fork。目标与哨兵是首结果验证，不是整场原生部署结论。

## 4. 最新上游对照与证据收集

上游日志改用异步缓冲文件，读取 Godot 日志不能再取得完整 RESULT/ACTION。第一次直接复制独立日志只得到前缀，未冒充完整动作对照。随后尝试在首结果请求末尾导出战后问题包；未修改的上游请求 `45f433b9306e4545b22ed18af4a0951d` 在搜索完成后因缺少 `replay/recent/session.json` 失败，失败请求和可取得的搜索记录分别保留，未标为 Passed。

最终采集改为只读持有当前战斗日志文件句柄，下一次建局使旧日志正常退役、关闭并刷出缓冲后，再读取该句柄。最后加一个仅捕获根的请求完成末份日志退役；没有修改生产日志代码或搜索预算。


| 最新上游对照 | 上游 runId | 合并版 runId | 完整动作 |
|---|---|---|---|
| short | `bdf47f6589014329997f292485c07025` | `ebb2fce7ed124f79a13cd14ad798ec39` | 57 条一致 |
| sentinel | `c065f7cf7b9847da967e022940036c0d` | `d8437a044f4242909b42cf20328d6b9f` | 9 条一致 |

两版的这四个请求均 Passed。目标 RESULT 的 **81/90 字段相同**，包括相同的主搜索 10,000 展开 / 144,276 转移 / 101,420 选择、评分 −2,570,057、预测战损 4、两瓶药水与 57 条完整动作。另 9 项差异完整保存：6 项是已准入动作调度诊断，3 项是共享时间窗内累计展开/转移/选择；哨兵的 **90/90 字段与 9 条动作全部一致**。

目标 Short 总展开为上游 13,307、合并版 34,770。`CombatSearchCoordinator.RunSupplementalAudits` 从同一 20 秒请求时限扣除已用时间，为药水反事实审计设置剩余期限；更快的主搜索留出更多审计时间。两版预算相同，但累计物理工作不相同，因此这组不用于等工作量速度倍率。原始比较、分类依据和所有动作均在结构化证据中；两份仅用于日志退役的根捕获请求不计为性能样本。

## 5. 复跑与范围

排序筛查从独立工作树的 `3f2ac26` 构建基线；候选仅应用 JSON 中的 `prototypePatch`。两份产物使用[前次固定输入与命令](perf2-integration-20260909.md#7-复跑)，按本报告 B–C–C–B、每次先 Short 预热再测 Short/normal，不与最新上游合并版混测。三个输入文件使用 `coverage/unattended/search-performance-necrobinder-projected-{run-cards,relics,potions}.json`。

本轮没有新的可见 Steam、Windows 游戏或整场部署结论。已有分支的性能与正确性证据继续按各自源码基线保留；本轮停止追加未证明收益的原型。Profiler、日志、诊断包和构建仅放在忽略目录。

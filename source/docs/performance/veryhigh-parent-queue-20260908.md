# 极高配置：有界父节点队列（2026-09-08）

[返回性能目录](README.md) · [结构化证据](veryhigh-parent-queue-20260908.json)

本轮以 `5dedbae` 为基线继续优化死灵法师药水压力场景。最终保留并行父节点调度改动：正常配置的单样本耗时 45.660→39.915 秒，减少12.58%（1.144倍），独立进程峰值RSS增加9.49%。**没有达到用户提出的再快一倍目标。** 没有调整搜索预算、Beam、评分、剪枝配额、DOP或GC政策；仍只用极高预设做性能A/B，普通开发提交，不升版本或发布。

## 为什么药水场景昂贵

输入包含38张牌、20件遗物及弃牌重抽/生成无色牌两瓶药水，要求至少使用一瓶。弃哪些牌、重抽后的手牌和后续出牌顺序共同扩展状态空间；生成牌又增加选择层。十张手牌理论上就有1024个弃牌子集，实际代码有分支配额，不能把理论数当作实际枚举数。最终返回路线虽然只用无色药水，搜索仍会评估另一瓶的候选。

这组输入没有找到可提前结束的胜利路线，会继续原有深搜与窄搜恢复。正常配置累计46,239展开、636,428转移、431,140选择分支，约35.5GB累计分配。短搜阶段测量中，累计worker耗时为Fork约5.60秒、动作约19.67秒（其中直接药水执行约3.47秒）、快照约15.04秒、剪枝约1.90秒；这些是跨线程累计且含嵌套阶段，不能相加换算墙钟百分比。成本分散在状态复制、卡牌/回合结算与评分指纹，只优化药水调用不足以翻倍。

## 最终实现与所有权

旧实现每组最多DOP个父节点，coordinator自己模拟一个，再按序提交；即使其他lane空闲，也要等整组结束才能接下一组。现在在同一内存预约窗口中最多准入`2×DOP`个父节点，由最多DOP个固定后台lane消费；coordinator从完成队列接收结果，归并该worker指标后先派发窗口内下一任务，再提交已接收连续前缀。较慢的父节点不再阻止空闲lane处理后续已准入工作。

候选和父节点仍按原输入顺序提交，转置、剪枝与预算由coordinator独占。原始批次使用独立lease；所有已准入父节点都完整计入现有内存预约，窗口不是固定字节界。异常停止继续派发，补齐未派发计数，排空工作后才释放批次和根。额外父节点lane参加缓存清理及Dispose；自然单父节点仍沿用coordinator加空闲lane的动作/选择回放，不嵌套外层并发。详细诊断和增量模式继续DOP1。

职责仍在`ParallelExpansion`与`Phases`，没有新增战斗状态或改变药水结算。架构地图、性能skill和两端结构门禁同步说明队列、按序接收及额外lane的生命周期。

## 正常配置对照

两版各自新建headless进程，先跑同一10,000节点短搜预热，再跑正常极高配置。DOP8、NoGC配置16GB、详细诊断/阶段计时/增量关闭，每个请求120秒上限，首搜索结果停止；每次请求明确固定独立artifact。性能期间没有编译或其他基准并发。

| 指标 | 基线 `adc1976b425041a291e8e5989f132a0b` | 最终 `b160b6539cd44ed39ea8aa937b7e89c9` |
|---|---:|---:|
| 总搜索墙钟 | 45.660秒 | 39.915秒 |
| 累计worker分配 | 35.467GB | 35.417GB |
| 100ms采样进程峰值RSS | 16.735GB | 18.323GB |
| 展开/转移/选择 | 46,239 / 636,428 / 431,140 | 相同 |
| 返回阶段/投影战损/仅死亡路线 | Deep / 9 / 是 | 相同 |
| 最终预测动作、目标和选择摘要 | 64步 | 完全相同 |
| GC累计暂停/最长观测暂停 | 1789.207 / 1734.718ms | 75.555 / 28.766ms |

GB使用十进制。累计分配仅减少0.143%，高分配问题依然存在。两版均按相同预热流程测量，但只有一组正式样本，GC暂停不同，不能保证稳定12.58%或严格不超过10%内存增幅；没有改GC策略，暂停差异不能单独归因于本轮。headless帧间隔不代表渲染卡顿。两版最终非时序搜索/质量字段一致；wave、action replay及deferred层数量会因新的分组方式变化，不声称调度指标相同。`Passed`是请求完成，不代表此药水输入打赢。

## 撤回的实验

- 仅卡牌预览监听失效时保留Power视图：短搜6.950→7.469秒，分配不降，撤回。
- 跳过只用于构造下一选择层的PendingChoice完整快照：在早期队列版本上6.116→5.883秒，额外收益约3.8%、分配仅少约26MB。扩大了回放/快照接口与验证面，收益不足，完全撤回。
- 早期队列固定短搜为6.950→6.116秒，10000/144368/101808工作量一致；最终队列另显式使用接收位图并在串行提交前补发任务，最终结论以正常配置表为准。冷启动预热11.832→11.040秒也不能混进正式倍率。

## 正确性与完成范围

- 最终`SearchPolicySnapshot`：`5eb810366694428eb44d15e7427c63ca`，Passed。250节点固定工作量的DOP1/DOP2动作、评分、续用、展开/转移及非时序剪枝等价，实际并发至少2；取消工作记账与冻结设置合同也通过。未把此合同时间用于性能。
- 最终机甲骑士整场哨兵：`f272c7ba66994d93b811a9001237a709`，Passed，战损8、57HP、第7回合结束、无药、零计划外重算；27条实际部署卡牌/目标/选择摘要与上一轮基线完全一致，Instant/0秒。
- 最终Release构建0警告/0错误，Bash及PowerShell结构门禁通过。已撤回的快照/Power实验不在最终产物中。
- 依用户要求跳过可见会话；没有运行Windows游戏、逐转移增量、全量覆盖或发布门禁。药水输入是注入的压力组合，不是原存档完整回放。

复跑输入沿用[压力报告](veryhigh-pressure-survey-20260908.md)及`coverage/unattended/search-performance-necrobinder-projected-{run-cards,relics,potions}.json`。结构化证据包含本轮命令，去掉个人artifact/evidence目录后可指定自己的构建目录；药水合同采用同一建局加`--verify-search-policy-snapshot --stop-after-combat-root-snapshot-assertion`。

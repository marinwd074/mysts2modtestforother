# 选牌续执行批量实施（2026-09-14）

用户要求完成[研究方案](choice-continuation-expansion-20260914.md)的三个阶段：卡牌、药水、回合触发与嵌套。基线为`7dd46d7`。**三阶段已完成，合同、三场完整搜索质量与原生部署均通过。** 保留搜索预算、候选原序、完整动作、分支所有权及既有部署语义；不启动可见Steam、不提升版本或发包。用户随后明确：决策质量未下降且未发现具体bug时，无需继续深挖工作量差异；因此保留计数差异，实际请求耗时与同工作量提速分开报告。

## 第一阶段：41张卡牌的自身选择

已有共同暂停位置推广至41张原版单人卡。仍限定单次手动执行、无附魔/污染、单层作用域及可复制状态；复杂触发暂时保留完整回放，后续阶段继续处理。

- 保存已经建立的选择请求和spec，候选与来源牌堆通过同一Fork context重映射。恢复直接调用原选择解析，避免探寻打击在GetSpec中再次洗牌候选并消耗RNG。
- 按重映射后的CardPlay复制格挡金额及powered事件计数。引擎没有单独的预测BlockGained历史类型，不新增虚构历史。
- 历史后缀补齐随机候选选择、生成候选、生成/完成配对及费用随机化；不在牌堆里的候选wrapper也通过同一context复制。继续共享此前已完成历史。
- Search使用同一资格入口，保持父节点/完整动作匹配、frontier排空、取消传播及嵌套回退的计数合同；原选择应用与出牌尾部仍只有一个实现。

| 验证 | 直接证据 |
| --- | --- |
| `CARD-CONTINUATION-EXPANDED` | `15ed72aac4504a0f8c56133251fe1c2c` Passed；41来源的82普通/升级分支，80种实际选择共380个候选、2种无选择；完整状态/历史/RNG、CardPlay/trace身份、兄弟修改与再次访问、DOP2、完成后Fork；10种代表原生结算 |
| 原三牌边界合同 | `18085ba4d6b044b58a0aef69bb4605b9` Passed；原生差分、真实洗牌、已有历史、升级、附加状态拒绝、嵌套回退、DOP2及释放 |
| 扩展真实搜索 | `5e70bf69886741e6abdc0dd97bfce87c` Passed；真实检查点逐分支对账、旧路径/DOP1/DOP2完整结果一致、同父并发取消/异常排空 |
| 扩展严格增量搜索 | `151a8484b2094687a8e28780453c855b` Passed；生产搜索实际复用与回退均命中，并从根完整回放逐项对账 |
| 结构门禁 | Bash与PowerShell均通过，`search_files=92`；新增请求重映射、格挡复制和禁止重新GetSpec的共同边界 |
| Release | 第一阶段引擎合同构建10.97秒、搜索合同构建10.86秒，均0警告/错误 |

所有请求上限120秒；全卡合同耗时66.483秒，原三牌29.528秒，扩展搜索4.584秒，严格增量4.248秒。前两次全卡建局分别因测试牌ID `GOOD_INSTINCTS`、`CLEAVE`不属于当前原生目录而失败，未执行续执行合同；更换为当前原生牌后取得上述证据，没有扩大超时或把失败算通过。

## 第二阶段：9种手动选牌药水

所有九种共用消费槽位、BeforePotionUsed和Use后的稳定检查点，选择应用、AfterPotionUsed与剩余补偿复用唯一完成入口。四种生成药水先从检查点执行原空选择probe，保留使用后钩子与读取候选的原顺序；其他五种继续从父状态枚举候选。串行和并行展开共享候选准备，frontier或串行枚举拥有前缀直到作业排空。

检查点使用普通Fork断言；生成候选历史只读共享，选中牌由既有Apply克隆。第三方类型、登记覆盖原版选择及不透明/事务StateStore继续完整回放。再次遇到嵌套选择时从原父完整回放，待第三阶段扩展。

| 验证 | 直接证据 |
| --- | --- |
| `POTION-CONTINUATION-CONTRACT` | `bcf7b7f30d5c4b0691c08c6707a3060f` Passed，30.220秒；9种41个选择、生产检查点50次分支/再次访问；完整状态/历史/RNG、药水消耗、BeltBuckle/ReptileTrinket后置触发、兄弟修改及DOP2、九种原生完整结算 |
| `POTION-CONTINUATION-SEARCH` | `856b8d5db4604d5fa9bb27e2070d1174` Passed，4.772秒；旧路径/DOP1/DOP2完整结果一致，同父并发、取消/异常排空，嵌套回退实际命中 |
| `POTION-CONTINUATION-INCREMENTAL` | `0d002f4702184b2da1e106666d60b131` Passed，4.155秒；严格增量逐项与根完整回放对账，复用及回退实际命中 |
| L0 | Release 11.15秒、0警告/错误；Bash/PowerShell门禁均通过，search_files=93；性能比较器14个测试通过 |

新增PotionChoicePrefixForks记录全部额外准备复制，Captures只记成功保留，Reuses包含四种生成药水原本已有的probe，Fallbacks记录恢复后增加的完整复制。物理Fork归一化扣除全部药水准备Fork和fallback，不使用Captures替代；worker合并/归零与日志、协议、比较器同时更新。以上都是无头语义/搜索合同，不是耗时收益基准。

## 第三阶段：回合来源与嵌套执行

4件手动选牌战斗遗物、5种Power的来源循环保存已物化来源、下一序号和明确阶段。低语耳环保持固定自动选择策略；24件战斗外遗物及多人指导仍在范围外。首回合和后续回合共用准备阶段机，保存抽牌、洗牌计数、历史补偿、提前SideTurnStart和自动出牌进度。

引擎把挂起选择及所有已适配外层操作保存为纯数据帧：抽牌/弃牌循环、监听者序列、Havoc/Cascade、重复出牌与DecisionsDecisions、出牌结算尾部。恢复继续原执行体，能够再次遇到选择并建立下一检查点。CardPlay、当前及已完成历史、非牌堆候选、Power来源与死亡集合共享一个Fork context；每帧恢复trace与抽牌深度，延迟历史必须与帧严格配对。普通Fork的事务断言保留，不保存Task、回调或CLR栈。

Search仅在明确父动作/已消费选择前缀匹配时复用，捕获和复制均由选择层或frontier持有、排空后释放。未知派发、未适配历史或不透明事务使该次优化回到原完整回放；不跳过效果，不增加搜索预算。`ExecutionChoiceReuses`替代原本的一次转移Fork，不能从物理工作归一化中再次扣除。

| 验证 | 直接证据 |
| --- | --- |
| 单层与嵌套抽牌/洗牌 | `fbdea0b824774494bc02e962669015d9`、`9deb4e09364545fab0195a71ef203273` Passed；完整状态/历史/RNG、部分抽牌、深层再次捕获、兄弟/DOP2及原生 |
| 回合开始来源 | 抽牌前两段及洗牌嵌套见下方部分请求说明；抽牌后六来源 `076aece95fa041bfad376621e623cef4` Passed；消耗/弃牌后的深层洗牌 `74e26c01154f473eaa443b322f08596e` Passed，均含原生逐字段对账 |
| 自动及重复子出牌 | Havoc/Cascade/DrawPrefix/Repeat独立完成项见下方说明；DecisionsDecisions `7e6063ff8bae4fa4af0d6e8d10094c17` Passed；每层再捕获、CardPlay别名、全部分支/DOP2及原生 |
| 生产选择层四组合同 | `e23c68bf438b429c90a50c00ba435724` Passed；全部回合来源92、Mayhem14、Cascade10、后续回合35个分支；状态键、continuation全部字段、历史/洗牌/一次transition与父/live隔离 |
| 真实动作与EndTurn搜索 | `e00560d9502d4faaaf1a6ccdbcbd59c1` Passed；旧路径/DOP1/DOP2完整结果及工作量、同父并发取消/异常排空；严格增量 `71cd4c23b66e499e9ae05aeb626f81b5` Passed |
| 首回合真实搜索 | `3b5c082e119e4414ae1187f82c45db30` Passed；三来源真实Solve完整结果一致；严格增量 `fe41c0e260a84ccfa2ec281013b21a4c` Passed |
| 原预算深链边界 | `402c6f1cc87444d99332248b15bb892c` Passed；九来源在原有限选择预算下，两模式均无完成根，Replay/Choice尝试完全相同；续跑实际命中，未扩大预算来制造成功 |
| 原生完整跨回合 | `47f1e0e03065438abb271475d31aca5c` Passed，26.745秒；真实EndPlayerTurnAction进入第二回合，35分支合同后部署最终连续选择，与完整预测StateText逐字段一致 |
| 最终共享尾部回归 | `b368988cf058462d8f52a1391f4d9e51` 全41卡，`4866f778cb404aadb7b590f05aa4c503` 全9药水，`e101b6d9dea14560a71c84d7f7a79800` 卡牌严格增量，`22148a231efe482cacadb8dbe965041a` 药水严格增量，均Passed |
| L0 | Release 11.32秒、0警告/错误；Bash/PowerShell门禁均通过，search_files=101；性能比较器16项通过 |

部分请求包含多个独立fixture，早期fixture完成后后续fixture失败：`dca025bbba3c47f4b2841534462de2e8`完成Before/BeforeShuffle，`53f6b33ec7a14b9b808d7adcb18de2c7`完成Havoc/Cascade/DrawPrefix，`1054681d024c4cb6891c1184f09cf3e5`完成Repeat。这些请求整体状态仍为Failed，只引用其明确记录的独立完成项。后续修正和相应通过证据均保存在JSON，不重复已通过且未受影响的fixture。

开发中修正了空历史后缀、Power身份映射、重复出牌的历史CardPlay模型别名和测试结算边界。首回合fixture还暴露出原有限预算无法展开九层来源，并单独保留等价边界合同；原生跨回合第一次因测试与自动准备逻辑争用选择会话超时，测试独占页面后在原20秒交接限制内完成。失败不计为通过。

## 最终联合验收

基线`7dd46d7`包含最初三张卡的优化，候选包含全部三个扩展阶段。测量前固定原蟹战、含生存者/深谋远虑的弃牌牌组、含攻击药水/无色药水/赌徒佳酿的场景，各按ABBA运行。使用原VeryHigh、DOP16、16GB NoGC及全部节点/选择/时间预算，`fixedSearchBudget:false`；每请求上限420秒，禁止测量时构建或运行其他测试。结果须比较完整动作、路线、政策及归一化工作量，并保留全部失败与性能反例。

首份基线A1蟹战完成；原候选B1在第68回合自动出牌前缀复制时失败：已经打出的能力牌离开所有牌堆，却仍在外层列表中。新增显式复制列表所持非牌堆wrapper及最小回归，`3b6ca0a3f7154dc2b99128822c6d9fc6`最小原生能力牌移除/连续选择合同通过；`c81dafb9cd84472db5e78a3bbb8f5b1b`旧抽牌前缀+DOP/取消/异常合同通过，明确关闭新路径以验证旧路径。Release 11.68秒、0警告/错误。不将失败结果用于收益结论。保留A1和失败B1，修正候选后继续预定对照。

修正后的F1/F2蟹战请求均完成，70个动作与路线一致，但总转移为2,312,844，基线为2,414,475，选择预算耗尽计数也不同。两份候选不具备同工作量收益资格；原配置请求耗时为129.962/132.070秒，均值较基线下降15.99%，同时分配增加0.81%、峰值RSS均值增加2.68%。这些是本场实际搜索结果，不能归因为相同工作上的纯执行提速。后续弃牌输入因把储君卡GLIMMER放入静默池而在建局前被拒绝，已改为原生SURVIVOR；失败输入单独保留。

归因运行D0只关闭第三阶段执行续接，保留前两阶段及全部共用执行体；完整原蟹战的动作、路线和全部逻辑工作字段恢复为基线值，总转移2,414,475。差异位于最终采用的两瓶药水搜索层。逐步审计同时检查完整状态、待选请求/候选、历史数量、洗牌及无效分支的两个方向；独立两瓶药水搜索的5,000节点（13,466次续接）与20,000节点（56,211次续接）均通过，并与关闭续接的完整搜索结果一致。额外诊断在32,665节点完成，但没有协调器的无药基线，不等同于原100,000节点搜索层。此前无药审计也不覆盖该层。未发现具体状态错误后，按用户要求停止进一步归因；全部诊断均不计入性能成绩。

首回合原生页面后规划合同`acb7c731cc3547569e2ca2fcd0e638ef`通过，计划包含至少三次准备选择；该请求在规划断言后停止，不作为实际应用整条选择链的证据。原生完整跨回合状态对账见上表。

三场12份有效测量已完成，每版本每场两次，完整动作/路线及其余质量字段均一致。蟹战沿用A1/F1/F2/A2，修正输入后的两场使用新的ABBA序列；无TimeLimit样本。药水场景另有一次幕编号错误：FUZZY_WURM_CRAWLER_WEAK属于原生`actIndex:0`，错误的`1`在建局前被拒绝；只修正幕编号后重建整组对照，失败样本保留。

| 场景 | 质量与工作量 | 平均耗时：基线→候选 | 累计分配变化 | 峰值RSS均值变化 |
| --- | --- | --- | --- | --- |
| 原蟹战 | 70动作、T15、战损40、两药相同；逻辑工作量不同 | 155.954→131.016秒（−15.99%） | +0.81% | +2.68% |
| 扩展弃牌 | 39动作、T1无伤；全部逻辑字段相同 | 4.163→4.281秒（+2.84%） | −1.83% | −1.35% |
| 携药轻场景 | 5动作、T1无伤；全部逻辑字段相同 | 0.593→0.595秒（+0.22%） | −2.73% | −0.24% |

轻场景没有建立耗时收益；不能用总分配下降声称所有场景提速。蟹战累计GC暂停均值13.904→8.672秒，但单次波动较大，且峰值RSS增加，不能外推可见卡顿改善。两个轻场景的GC暂停均为0。此处比较三阶段合并结果，不归因单张牌或单种药水，也不把未实际命中的检查点算作该来源的收益。性能构建之后只增加默认关闭的测试审计入口，最终正常Release用于原生部署。

候选蟹战选中结果记录893,641次嵌套/回合执行续接；扩展弃牌场景卡牌前缀复用32,059→51,895次。三场选中结果载体的药水前缀复用计数均为0，未证明药水检查点的独立整搜性能收益；九种药水的功能和真实搜索命中由第二阶段专用合同验证。计数属于选中SolverResult载体，耗时与分配则覆盖请求内全部搜索层，不能混用两种统计范围。

最终正常Release构建11.22秒、0警告/错误。原生整场部署`ef6fd35b159a4aee974c826319755371` Passed，60.440秒：39动作计划实际执行到T1胜利，HP56→56、敌HP0、计划外重算0；固定Instant/0秒、请求120秒。已将最终正常DLL与manifest复制到本地Mod目录，不提升版本、创建ZIP或推送远端。

最终Bash与PowerShell结构门禁均通过，`search_files=101`；变更空白检查通过。执行计数接入后的16项比较器测试此前已通过且源码未再修改，复用该证据。

全部输入、设置、12份样本、完整动作/路线、逻辑计数差异及开发失败见[结构化证据](choice-continuation-expansion-implementation-20260914.json)。按用户澄清停止工作量归因；蟹战的差异仍明确保留，没有宣称任意战斗或所有嵌套状态均已穷举验证。

# 六个后续方向：诊断与逐项开发（2026-09-13）

六项均已完成诊断、候选开发和性能对照。保留选牌构造/评分、EndTurn首层回放、转置前沿存储及快照覆盖汇总；父状态Fork缩锁和空派发扫描缓存均在对照后撤回。各项证据与限制如下。

## 新基线诊断

基线 `37fc6c2` 已包含普通卡牌/Power克隆与提前EndTurn。真实BaseLib 3.4.7 / Ritsu 0.5.20、Linux无头、固定3万节点、DOP16、16GB十进制NoGC区域，首个主搜索。临时诊断源码在构建后完整恢复，不进入生产。

`33fab65e4eab4f23a1630668fa6fb9cd` Passed，85项质量/总工作字段及22步完整路线与未插桩结果相同。线程局部计数与阶段计量用于定位；计时包含等待，嵌套阶段重叠，不能按占比直接承诺提速。

| 子路径 | 次数及成本 | 含义 |
|---|---|---|
| 选牌构造 | 49,578次；生成425,434份组合，保留380,460份；累计869,052,520B、2.956线程秒 | 保留量含额外物理实例代表，不能简单把二者之差都视为无用组合 |
| 组合评分 | 1,114,839次；117,853,704B、0.492线程秒 | 是选牌构造子集；大多数单选转变也反复评分，并非只有任意子集才值得检查 |
| 原递归组合 | 550,324次递归、1,221,211次同层语义键比较 | 可预计算最近相等位置，保留原遍历和每张数上限 |
| EndTurn初始回放 | 29,999次、15.888线程秒、1.398GB | 17,506次随后有挂起选择（58.36%），12,493次无挂起 |
| EndTurn选择解析 | 174,512次迭代器MoveNext、58.287线程秒、9.297GB | 包含结束迭代的调用，计时排除yield后的消费者；不能当作独立分支数或纯CPU |
| 同父Fork锁 | 380,480次，累计持有26.388线程秒、等待10.664线程秒 | 提前Tail后，其回放也进入这个gate，范围与更早的仅卡牌/药水诊断不同 |
| NextJob | 296,238次、0.787协调线程秒、37.328MB；扫描父槽7,954,957次 | 下一候选是缓存协调线程拥有的就绪状态，保持原优先级及轮询次序 |
| Snapshot | 41.494线程秒、2.354GB；其中Fingerprint16.774秒/564.599MB，Threat3.621秒/329.969MB | Fingerprint包含ProjectedShuffle5.048秒/23.762MB；阶段相互包含，后续继续细分未归类部分 |

展开窗口15.013秒；Tail作业75.197线程秒，协调线程等待11.703秒、提交1.206秒。全部原始分布和阶段字符串见[结构化证据](six-directions-20260913.json)。

## 1. 选牌组合构造与重复评分

### 实现与所有权

在原 `CardChoiceSupport` 内完成两项单因素实验：

1. 预计算每个排序后语义键最近的相等位置，将原递归 `[start,i)` 扫描变为整数判断。128项以内用栈上空间，更大输入用独占数组。各张数直接追加到同一结果表，用本张数起点限制生成数，保留第一个张数代表；复用递归临时列表，每份完成组合有独立只读数组。
2. 每份完成组合持有只在本次BuildChoices中有效的惰性评分。第一次实际需要时执行原评分公式，后续排序/身份代表消费复用它；未发生评分的路径继续不评分。物理实例补充创建自己的组合，按原公式处理。不在卡牌、节点、spec或全局字典缓存结果。

排序算法、同分顺序、原组合限额、各张数代表、固定多弃牌多样性、物理实例补充和最终令牌序列均保留。没有改变评分浮点求和次序或生产搜索预算。

### 正式实验A：组合容器及重复位置

以 `37fc6c2` 为基线，候选只改组合构造。真实BaseLib，四个新进程A-B-B-A，固定3万节点、DOP16、16GB区域；无插桩、无阶段计量，计时不含启动/恢复。

| 样本 | 搜索秒 | 累计分配GB | GC暂停ms | 最大GC暂停ms |
|---|---:|---:|---:|---:|
| A1 | 23.8787 | 23.2199 | 1864.887 | 1838.969 |
| B1 | 23.7796 | 23.1649 | 1907.789 | 1881.519 |
| B2 | 24.1564 | 23.1626 | 1924.408 | 1896.733 |
| A2 | 23.8729 | 23.2140 | 1853.768 | 1826.246 |

平均累计分配少53.221MB（**0.229%**），两对均下降。耗时23.8758→23.9680秒（**增加0.386%**），配对方向不一致；GC累计和最大暂停均值分别增加3.05%/3.08%。保留为分配削减，不宣称提速或GC改善。

### 正式实验B：惰性评分复用

以实验A的组合候选为基线，仅叠加每份组合的惰性评分复用；其他口径相同。

| 样本 | 搜索秒 | 累计分配GB | GC暂停ms | 最大GC暂停ms |
|---|---:|---:|---:|---:|
| A1 | 23.7591 | 23.1509 | 1919.487 | 1893.121 |
| B1 | 22.4820 | 23.1112 | 41.760 | 21.120 |
| B2 | 23.8798 | 23.1165 | 1875.976 | 1848.217 |
| A2 | 23.6781 | 23.1803 | 1986.413 | 1956.022 |

平均累计分配再少51.722MB（**0.223%**），两对均下降。耗时23.7186→23.1809秒，均值少2.267%，但两对分别更快/更慢，B1的GC暂停只有41.760ms，远低于另外三次约1.9秒。**没有建立稳定提速，也不把GC均值约减半当作稳定收益。** 保留为重复评分与分配削减；不剔除B1，不扣除GC后重算宣传数字。

这两组基线不同，不能把耗时或分配百分比相加当作最终组合实测。八次均Passed、NodeLimit、最大并发16，无NoGC丢失。每组85项质量/总工作字段及22步完整路线一致，11项自适应调度字段单列。端点内存原样存入JSON，没有稳定峰值内存结论。

### 合同与限制

- 初版组合合同 `0e73c2914a044895b92f59926d262f9b`、评分候选合同 `263236af5d8c43fdb693cee126d38543` 均Passed。1200个原生模型输入，5008个输出选择，逐份比较完整序列化结果；覆盖所有PlanChoiceEffect、重复引用/同名不同实例、升级/免费、来源别名/副本/不同列表、张数和MaxBranches边界、隐式全选与129项输入。
- 最终合同 `4707d04d6b2b43fab603c3f97a0c3df5` Passed，额外覆盖虚无、保留、不可打出、Sly和调用间同一卡牌的状态变化，live续用文本不变。旧构造/递归算法冻结在Testing partial，评分公式及身份规则共用未改语义，避免新实现自证相等。
- 最终 `STAND-PAT-MEMORY-BOUNDARY` / `58916b8833cd4ad19355cec64fd0ef19` Passed：DOP1/DOP2完整结果与工作量、104MiB人工压力、取消/注入错误排空和同根复用。
- 原生局部探针每组300次：初版单选分配5,100,000→4,924,800B，固定选2为11,678,400→11,246,400B，任意0–5为39,175,200→35,373,600B。评分候选对应4,255,200 / 8,128,800 / 33,688,800B。它们属于旧1200输入合同中的局部形状，不是整场收益。任意子集时间有明显分层JIT漂移，未将局部毫秒当作提速证据；最终扩充关键词后另有原样保存的数据。
- 生产Release构建零警告零错误，Bash与PowerShell结构门禁通过（均在Linux执行，87个Search文件）。
- 全部仅Linux无头；旧归档Continuation匹配但native编码不可比较，生产仍保留MVID门。没有可见游戏验收、Windows部署或版本发布。

## 2. 单个EndTurn内的首层选择回放

复用原`PendingChoiceReplayLayer`和`PrimaryChoiceReplayFrontier`。准备作业执行初始EndTurn，只有挂起选择通过原增长限制、且F/R均至少为语义兄弟数N、N至少为2时，才交出原预算保证必经的首层回放。每个动作保留完整TurnStartChoices及无效分支标记；初始挂起模拟器及时释放。回放由固定lane执行，所有首层结果到齐后，唯一续接在原遍历位置扣逻辑配额、解析嵌套选择并收集/回放实体补充。最终候选和待命基线仍在全部兄弟作业完成后发布。同父Fork gate、每项最多4次回放及512上限未改。

以第1项最终生产代码`17e0977`为基线，真实BaseLib、DOP16、固定3万节点、16GB区域、四个新进程A-B-B-A；无插桩或阶段计时，首个主搜索口径。

| 样本 | 搜索秒 | 累计分配GB | GC暂停ms | 最大GC暂停ms |
|---|---:|---:|---:|---:|
| A1 | 23.4848 | 23.1211 | 2071.204 | 2045.140 |
| B1 | 21.7812 | 23.1693 | 44.161 | 21.603 |
| B2 | 23.3578 | 23.1706 | 1993.468 | 1965.563 |
| A2 | 24.7376 | 23.1212 | 4274.088 | 2275.120 |

平均24.1112→22.5695秒（少**6.394%**），两对均更快；累计分配均值23.1212→23.1700GB（**增加0.211%**）。但B1只有44.161ms GC暂停，A2达到4274.088ms，A基线自身耗时漂移5.33%。**保留已验证的有界并发路径，但没有建立稳定6.39%提速或GC改善；不删除极端样本、不扣GC重算，也不跨前几轮相加。** 四次均Passed/NodeLimit、最大并发16、NoGC丢失0；85项质量/总工作字段及22步完整路线一致，调度字段单列。

合同`END-TURN-CHOICE-REPLAY` / `e97bc6d2d32044fe8ce45f8ab0639667` Passed：只在捕获后的隔离根注入ToolsOfTheTradePower，使下一玩家回合有弃牌选择；第一个同父回放在观察点等待另一个lane，证明实际重叠。覆盖取消、原异常传播、所有回调退出、部分工作计量、同根复用和DOP1/DOP2完整结果一致，live文本不变。`STAND-PAT-MEMORY-BOUNDARY` / `ce5ca25740a142c7a667ecb0bfb9482b` Passed，包含104MiB人工压力、排空和同根完整等价。

前三次定向建局未触发目标观察点，均按Failed保留在JSON；其中两次使用WellLaidPlansPower的隔离根仍未触发，不能把它们当作该Power通过证据。改用已登记的下一回合弃牌路径后通过，没有扩大200节点测试预算或120秒请求上限。生产Release零警告零错误，Bash/PowerShell结构门禁通过；仅Linux无头，未做可见验收或Windows部署。

## 3. 父状态封存与Fork锁：原型撤回

在`b0d684b`上做了源对象资格诊断和实际缩锁原型。诊断`a978c6db9463467a975ca306ac215d10` Passed：419,815次模拟器Fork中，仅16,811次没有遇到现有独立克隆门禁拒绝的模型，403,004次包含未获放行的`VigorPower`。StateStore中还实际调用了活力、Automation、CardPlayPair、Panache的Fork；不能从普通卡牌/Power克隆已放行推导整个父状态都只读。

[完整实验补丁](experiments/frozen-parent-fork-b0d684b.patch)基于`b0d684b`，**没有应用到生产**。原型让每个已准入父节点的首次必要Fork在原锁内完成，借同一ForkContext检查实际复制过的源对象，并利用这次原本必需的历史封段；没有额外制造分支或多调用第三方Assert/Fork。只有精确绑定的父快照、无StateStore回调、已审核的原生Power、无未知对象/Orb、附加模型安全的卡牌才允许后续同父复制移出锁。未知项保持整段原锁；COW的volatile共享位仅在父状态不再变化的准入生命周期内幂等置位。原型包含lane局部计数，资格检查成本也计入B，不把插桩候选描述为无插桩生产测速。

原型诊断`4c61527946db4e7fb52cc0e00e164b08` Passed，14,070次移出锁、366,180次保持锁，仅**3.70%**。不放宽第三方边界去追求覆盖率。更细地把任意StateStore回调与容器复制交错执行，仍需新的源对象不变性契约；本次没有以一组原生模型结果代替那项证明。

随后真实BaseLib、DOP16、3万节点、16GB区域，四个新进程A-B-B-A：

| 样本 | runId | 搜索秒 | 累计分配GB | GC暂停ms |
|---|---|---:|---:|---:|
| A1 | f89478114f0342a1ab2e5c9a6388eefd | 23.5184 | 23.1650 | 1866.046 |
| B1 | 136c91c909154e02b515ef24a2d1e75a | 24.1430 | 23.1763 | 2217.568 |
| B2 | 86de60beadc24dc181e9384963fca92d | 23.7920 | 23.1796 | 1877.563 |
| A2 | 4745b80b22a840d9a9dbc5d7b8a20015 | 22.5925 | 23.1923 | 40.001 |

平均23.0554→23.9675秒（本组**慢3.956%**），两对均更慢，分配变化方向不一致。A2仅40ms GC，其余约1.9–2.2秒，不能把均值当作稳定回归幅度；但本组没有支持采用的提速或分配收益。两个B都放行14,070次。四次Passed/NodeLimit、最大并发16；85项质量/总工作字段和22步完整路线一致，调度与运行时字段单列。资格诊断不是第三方并发合同；因为候选撤回，没有将未执行的取消/第三方缩锁合同记成通过。原型构建零警告零错误，源码在构建后恢复。**保留现有Fork锁；第3项以已开发、测速并撤回的候选结束，而不是宣称消除了父Fork等待。**

## 4. 小区域存活对象：单标签转置前沿内联

当前基线`b0d684b`，真实BaseLib、DOP16、1GB、4万节点；诊断`3a3c0eac7130466cb03e12459fe7caca` Passed。记录全部71次回收的前后live/heap/fragmentation/committed、回收经过时间、GC暂停和区域恢复状态，并在第3个已排空检查点采集一次heap dump。阶段插桩和dump会扰动耗时，68.556秒仅作为诊断原始值，**不与正式A/B或旧79秒样本直接比速度**。完整[逐次指标和存活类型摘要](six-memory-20260913.json)已保留；完整dump/日志在本地，不进入源码。

71次回收后`managed_live_after_collect`为111.642MB–1,270.536MB，回收经过时间合计30.563秒；这是含后台收集等待的时间，与2.588秒GC暂停不同，也不能叠加到总搜。第3检查点dump的非Free对象载荷147.072MB、Free块698.278MB；它们不是`GC.GetTotalMemory`、提交内存或工作集的同义量。该点有1,550个模拟器、2,944个快照、2,945个SearchNode，未见存活的AdmittedExpansionJob/AdmittedParent/AdmittedJobOutcome。GC根证明两个转置字典由SearchRunContext持有，抽查的历史快照经SearchNode父链存活。循环规划仍读取祖先耐久等元数据，所以没有清空这些字段；仅凭等待循环的局部变量也不能断言邮箱泄漏。**这是一处已排空点的对象图，不是所有71个点都做了对象级根分析。**

发现36,190个TranspositionFrontier全部只有一个标签，仍各带List和单元素数组：前沿对象868,560B、List 1,158,080B、标签数组2,026,640B。把唯一标签内联到前沿对象，多标签才建立原序List；接受新标签后若重新缩为一个，立即丢弃额外容器。六维Dominates、重复拒绝、扫描/移除/追加顺序保持原样，没有清空判重表或改变搜索预算。职责移到`CombatBeamSolver.Transpositions.cs`，便于合同直接链接实际生产源码。

固定随机种子的512,005项纯合同覆盖不可比标签、重复、替换、NaN/Infinity、收缩与再扩张，与冻结的旧List算法逐步比较。10万条保留单标签的分配**11,200,000→5,600,000B**；这是对象表示证据，不是整搜减少50%。`STAND-PAT-MEMORY-BOUNDARY` / `c37135f70b104acd95d31c47ae52c9ee` Passed，包含DOP1/DOP2、104MiB人工压力、取消/原异常排空和同根复用。生产Release零警告零错误，两端结构门禁通过（89个Search文件，均在Linux执行）。

以`e5dac11`（生产等同`b0d684b`）为基线，真实BaseLib、DOP16、1GB、4万节点、四个新进程A-B-B-A，无阶段插桩或heap dump：

| 样本 | runId | 搜索秒 | 累计worker分配GB | GC暂停ms | 最大GC暂停ms |
|---|---|---:|---:|---:|---:|
| A1 | 26a710fc49fe4c0b92dbe8eb3a135b05 | 63.6732 | 39.3335 | 3448.134 | 1304.357 |
| B1 | 8b9b141d9426478281b3db2944374581 | 60.1379 | 39.3268 | 3521.279 | 1312.230 |
| B2 | bb37fbd7f83f4aaab20c5ac891bec080 | 63.1105 | 39.3148 | 3310.173 | 1399.890 |
| A2 | cacaf027bcd748068fc4d8b63630246a | 66.3716 | 39.3442 | 2440.690 | 1453.258 |

平均**65.0224→61.6242秒（少5.226%）**，两对均更快；累计worker分配均值39.3388→39.3208GB（少约18.1MB/0.046%），不能把coordinator存储的局部节省直接按这个指标放大。四次都是**71次回收、72次区域启动、NoGC丢失0**。回收经过时间均值31.129→29.109秒，但GC累计暂停均值2.944→3.416秒（更差），最大暂停均值1.379→1.356秒。保留对象表示优化与本组整搜结果，不宣称降低了回收次数、稳定GC暂停或可见游戏卡顿。四次Passed/NodeLimit、最大并发16，85项质量/总工作字段及22步完整路线一致；各次71条回收记录完整，调度/运行时字段单列。没有Windows部署或可见验收。

## 5. 快照内部：覆盖风险结果延后物化

第4项当前基线诊断同时细分了665,265次Snapshot。以下是同一1GB/4万节点诊断中的协调/worker线程内经过时间，含插桩与嵌套调用，不是墙钟占比；CardValue计时包含在所属阶段内。

| 阶段 | 线程内经过秒 | 累计分配MB |
|---|---:|---:|
| 状态/牌堆指纹（不含洗牌投影） | 14.161 | 856.629 |
| 洗牌投影 | 8.980 | 35.784 |
| 手牌可达性 | 6.438 | 454.472 |
| 覆盖风险汇总 | 5.233 | 1032.852 |
| 威胁投影 | 4.049 | 498.247 |
| 战略效果 | 3.745 | 487.760 |
| 保留手牌 | 2.229 | 61.649 |
| 保留攻击价值 | 2.030 | 0 |

所有阶段、CardValue次数/耗时与分配保存在[诊断数据](six-memory-20260913.json)。例如洗牌已算21,361,227次CardValue，后续保留攻击又算5,489,141次；没有据此加入跨评估或跨分支值缓存。先处理可以保持原判定调用顺序的重复结果构造：`PredictionCoverage.Collect`原来为每个历史风险创建PredictionGap，再按SourceId/Method/Reason/Compensated去重。现在逐项按原方法取得四字段，用同样的键本地去重，仅首次出现时创建结果对象。原补偿判断逐项调用，SourceId/Method的稳定排序及同键先遇到者顺序未改；没有缓存第三方回调结果或减少未知风险。

`SNAPSHOT-COVERAGE-CONTRACT` / `f43fa39f50794e9899ca61ca7c068151` Passed：194次与冻结旧LINQ/描述方法的完整列表对照；包含空历史、null trace、所有已定义风险原因、补偿/未补偿、重复风险夹杂非风险事件、不同方法及64次Fork分段后的父子追加。最终111种gap；live续用文本不变。重复历史100次汇总的局部分配37,444,192→14,378,256B（约少61.6%），不是整搜收益。首版合同`a5ef136338e44c94b2584a3f8001817d`把根捕获放入后台隔离作用域，触发Power变量物化保护，Failed原样保留；改为先在主线程捕获根，再在隔离作用域Fork后通过，未改生产保护或增大预算。

`STAND-PAT-MEMORY-BOUNDARY` / `9c5c8b0aa26c4fe7b3619b2aee92daef` Passed；DOP1/DOP2、104MiB压力、取消/错误排空及同根复用通过。生产Release零警告零错误，两端结构门禁通过（89个Search文件）。随后以`e978356`为基线，真实BaseLib、DOP16、1GB、4万节点，四个新进程A-B-B-A：

| 样本 | runId | 搜索秒 | 累计worker分配GB | GC暂停ms | 最大GC暂停ms |
|---|---|---:|---:|---:|---:|
| A1 | bc02b392f2d54e88abdb18afbfd005d5 | 63.4381 | 39.3054 | 4244.166 | 1416.639 |
| B1 | bdc66503f14f4380a4a098eaa7d9d6c7 | 63.1765 | 39.2092 | 4572.685 | 1212.163 |
| B2 | ea37252e9756456ca40167bc52b6b3c9 | 64.9997 | 39.1835 | 2004.872 | 1044.792 |
| A2 | e58c589e875641029c568349e7e77cf0 | 65.9416 | 39.3181 | 4516.187 | 1486.922 |

平均64.6898→64.0881秒（少0.930%），两对略快，但小于A基线3.95%的漂移；累计worker分配39.3118→39.1964GB（少**115.407MB/0.294%**），两对均下降。四次仍71次回收，NoGC丢失0；GC暂停2.00–4.57秒，未建立稳定提速或GC改善。保留重复结果对象削减。四次Passed/NodeLimit、最大并发16，85项质量/总工作字段与22步完整路线相同。均值不与前几轮相加，未做可见或Windows验收。

## 6. 派发：缓存父节点的空选择扫描

在第5项基线上逐次核对原扫描，诊断`17c7614ecfd64189a74e1fa4b2696a78` Passed：8,680,884次FindChoiceJob查询中，7,941,370次可复用上次空结果；原算法读取67,884,053个frontier/probe槽，其中62,762,346次（92.45%）可省。每次命中仍执行完整原扫描验证，陈旧空结果0；85项字段与22步完整路线一致。外层父槽仍扫描9,721,257次，NextJob调用447,516次，计入验证与计数后的协调线程经过862.7854ms、分配56,760,216B；**此插桩时间不是优化前后计时**，外层扫描和邮箱唤醒没有消除。

实验候选仅为每个AdmittedParent增加一个空结果标记，完整原序扫描确认为空后才设置。FindChoiceJob仍返回原优先级下的第一个正结果；MarkDispatched及Receive入口清空标记，下一查询重新扫描。就绪条件只由coordinator这两处改变；worker对已派发快照的消费/释放不会使新的回放或续接就绪。没有缓存作业/快照引用、改变父轮询次序、增加唤醒或合批，原每作业最多4次回放保持。

最终`END-TURN-CHOICE-REPLAY` / `1360a320863b44b7959219a3a7709552`及`STAND-PAT-MEMORY-BOUNDARY` / `ad4117d4bdfb4acc8ee72b6979ee90bb` Passed，覆盖实际多lane回放、取消/异常排空、同根复用、104MiB压力与DOP1/DOP2完整结果一致。候选Release零警告零错误；Bash/PowerShell结构门禁通过（89个Search文件，均在Linux执行）。撤回后生产源码与已验证的`d0e942d`完全相同。

基线`d0e942d`，真实BaseLib、DOP16、16GB、3万节点，四个新进程A-B-B-A；无诊断插桩或并行构建：

| 样本 | runId | 搜索秒 | 累计worker分配GB | GC暂停ms | 最大GC暂停ms |
|---|---|---:|---:|---:|---:|
| A1 | f2fbebba60f34977a9cdcc8023aa1ffe | 23.1009 | 23.1422 | 1943.640 | 1917.230 |
| B1 | 9b6f440f1a6a46b4afff95d58ba16744 | 24.1305 | 23.1085 | 2332.094 | 2295.763 |
| B2 | 8018222430814700885b3f314045e7e4 | 23.9618 | 23.1315 | 1849.256 | 1822.535 |
| A2 | 5a003285a40248588efa02d819ff7b7b | 22.4353 | 23.1336 | 35.012 | 16.910 |

平均22.7681→24.0462秒（耗时变化+5.613%），累计worker分配23.1379→23.1200GB（变化-0.077%）。 四次均Passed/NodeLimit、最大并发16，85项质量/工作字段及22步完整路线一致。两对B均更慢，A2仅35.012ms GC暂停，其余约1.85–2.33秒，不能宣称稳定慢5.613%；但没有足够采用证据，**撤回生产缓存**。完整[实验补丁](experiments/choice-ready-cache-d0e942d.patch)基于`d0e942d`保留，生产恢复到第5项。未建立稳定整搜提速或GC改善，不把92.45%的内部槽读取减少写成整搜收益。原始计数、全量运行指标和合同结果见[结构化证据](six-directions-20260913.json)。

## 完成状态

六项均已完成诊断、候选开发及对照：第1、2、4、5项保留，第3、6项原型测速后撤回。每轮独立基线，百分比不能相加；没有最终组合相对上游的统一测速、可见帧率证据或Windows部署/发布。

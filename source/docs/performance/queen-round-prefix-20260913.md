# 女王回合前缀复用与10秒目标（2026-09-13）

**完整原包配置进入10秒的目标尚未达成。** 本轮保留抽牌后的回合前缀复用，在固定20000节点主搜索的A-B-B-A中平均耗时从23.500秒降至21.257秒（−9.544%），worker累计分配从19.473GB降至16.365GB（−15.959%）。该样本缩小了测试节点数并关闭药水审计，不能代替用户要求的完整请求。

## 验收与取舍

用户确认以原包自定义配置验收：Beam512、100000节点、每节点出牌100、牌堆42/手牌54选择、300秒搜索上限、Smart药水及原两槽，DOP16和16GB NoGC。生产预算、候选政策、评分和药水政策均未修改。所有数据来自Linux headless及真实BaseLib/Ritsu，不代表Windows或可见卡顿。

`c38a834`完整配置基线runId `0d3bcd11c0e14630a36135151b3da9c5`达到120秒请求上限，没有完整结果，采样峰值RSS28.043GB。本轮不延长该请求超时，也不将原先10000节点约10秒的结果作为达标。 最终候选runId `fca513d8d11a42e1a4dc6ac3406c0520`同样达到120秒请求上限，没有结果，采样峰值RSS22.690GB。两个样本均未完成，不据此计算完整搜索提速或同工作量峰值收益。

先调查完整动作缓存：10000节点单主搜索152494次转移只有372次重复状态/动作键（0.244%）；相同短节点Smart三层578800次转移有64384次重复（11.123%）。状态键不包含全部累计路线标签，不能盲目复用整个模拟或快照。未实施全局动作缓存。

采用更窄的复用：同一父节点EndTurn无计划选择的初探，在普通抽牌及历史补偿已经完成、尚无挂起选择时，保存一份独占模拟前缀。后续该父的回合开始选择从前缀复制并执行余下阶段，省去结束回合、敌方行动和普通抽牌的重复推进。只有存在ToolsOfTheTradePower时预留；其他来源、较早挂起选择和Knowledge选择仍走完整回放。

抽牌前原型10000节点10.162秒、抽牌后未限制捕获原型10.141秒，均没有短样本提速，未作为最终收益。20000节点诊断显示19419次捕获中12167次未使用、79305次后缀回放复用，因此最终按实际Power限制预留。诊断阶段累计scope时间含并行重叠、等待和GC，不能换算成CPU或请求节省。

## 所有权与验证

`RoundTransition`保存玩家回合开始的权威流程和前缀类型。原`AssertForkable`完整保留，捕获时仅临时关闭初探的空选择cursor并在finally恢复；不复制挂起动作/选择事务。复制源由EndTurn frontier拥有，父节点引用及回合匹配，同父Fork gate串行复制，每个worker独占子模拟；全部生产者和唯一原序续接排空后释放。新增计数分别记录捕获/复用，额外捕获仍计入物理Fork总数；测试比较扣除此额外项的转移Fork数。

`END-TURN-CHOICE-REPLAY` runId `b6e92887143440f893716e6aef406f4d` Passed：逐个首层选择比较完整回放与后缀回放的状态键、完整ContinuationStamp、评分、边界、死亡、风险、终局与gap；另对账历史条目数/洗牌数，写过其他兄弟后重访首个分支，检查父与live未变；原双lane取消、异常排空及DOP1/DOP2结果合同通过并断言真正捕获/复用。该合同没有开启整个搜索的VerifyIncrementalSearch，也不声称所有原生/第三方组合的差分覆盖。

| 固定20000展开 | A1 | B1 | B2 | A2 |
| --- | ---: | ---: | ---: | ---: |
| 总搜索秒 | 23.601553 | 21.300720 | 21.213796 | 23.398912 |
| worker累计分配GB | 19.491744 | 16.359160 | 16.371304 | 19.454188 |
| GC总暂停毫秒 | 2441.434 | 2285.071 | 2284.359 | 2452.414 |

基线先将原玩家回合开始代码纯提取为helper；10000节点对照84字段/28动作相同后再加入复用。每次新进程，正式产物无CPU/重复回放插桩，结果导出在搜索指标冻结后。83项非时序/非调度字段、40步完整动作及其余结果文本严格相同，扣除前缀捕获的Fork均349198；实际展开20000、转移349198、选择212548、8回合、玩家82HP/敌508HP。全部原始指标及runId见[结构化数据](queen-round-prefix-20260913.json)。均为NodeLimit部分路线，不是整场胜利或原10万请求质量结论。

正常Release构建0警告/0错误；Bash和PowerShell结构门禁均Passed（90个Search文件）。`STAND-PAT-MEMORY-BOUNDARY` / `e05a6b190efd4511ba2ff5b610dd3653` Passed，覆盖DOP等价、取消/失败排空与内存检查点。首次哨兵启动因产物目录缺manifest被启动器拒绝，尚未进入游戏；补齐manifest后执行成功。

## 后续边界

现有证据支持局部优化，但没有支持十倍以上提速的廉价缓存：单主搜索全动作重复仅0.244%，回合前缀只消除一部分重复推进。若继续针对剩余挂起选择消除回放，需要显式可恢复的命令续接、事务所有权、分支隔离及历史/RNG差分，这是模拟执行边界改造；当前仓库要求高成本方案先报告，不自行重写战斗流程。不能通过减节点、收窄选择、关闭药水审计或放宽相等判定来宣布完成。

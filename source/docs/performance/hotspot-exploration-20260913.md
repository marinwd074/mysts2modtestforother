# 合并后的性能探索（2026-09-13）

[性能索引](README.md) · [结构化证据](hotspot-exploration-20260913.json)

用户确认 PR #89 已合并，要求从上游新开性能分支并继续测试与探索。`perf/hotspot-exploration-20260913` 基于 `bcc15da`（0.37.0），带入上一分支尚未进入上游的最终压力报告。上游还改动了评分/威胁投影与卖血策略，因此本轮重新建立基线；不把旧分支数字当作新分支 A/B。

## 热点证据

正常生产 Release 基线构建通过，0警告/错误。原生药水投影根使用 VeryHigh 全部维度、DOP16、NoGC16GB，显式 FixedBudget 关闭无胜利后的扩展。CPU 采样 `dd4974e043914796b22de1bc7de02291` Passed：请求累计145,597展开、1,705,319转移、1,048,545选择；预测T12胜利、战损2、两瓶药。FixedBudget不等于只跑主搜索，药水审计与既有窄Beam恢复仍执行。

这是带采样的定位请求，78.463秒/94.086GB分配不作正常性能基准。采样从独立冷进程启动开始，`cpu-clock:u` 199Hz、frame pointer与JIT符号；共128,495样本，其中109,458条栈包含CombatBeamSolver，权重占全进程样本85.18%。缺栈2条、栈长至少127的30条；已识别搜索的self样本中17.18%未解析到libcoreclr内部函数，不能视为已完整解释全部CPU。采集器通过SIGINT结束（返回-2），perf报告写入128,495样本，JIT转换和导出成功；不把中断返回码写成正常exit0，也不声称独立EventPipe无丢事件。

| 已识别搜索CPU的互斥分类 | 占比 |
|---|---:|
| 快照估值 | 38.14% |
| 回合模拟 | 16.57% |
| 出牌执行 | 14.06% |
| 分支复制 | 13.41% |
| 其他搜索 | 7.25% |
| 其他回放 | 6.84% |
| 其他保路 | 2.28% |
| 药水执行 | 1.45% |

分类按Fork→Snapshot→AdvanceRound→ManualPlay等顺序归属，每条栈只计一个类别。可重叠的函数族占比分开：Hook15.31%、registry10.60%、监听表维护7.65%、状态键5.64%、模型克隆5.06%；这些数字不可相加，也不代表相应微优化的可兑现收益。

## 候选与成本

| 方向 | 成本及证据 | 决策 |
|---|---|---|
| 伤害修正列表按需分配 | 低：内部返回类型改为只读列表，四个阶段与小数截断/追加顺序不变；空结果使用不可变空数组，有元素才创建独占List | 原型及语义对照完成，整搜对照见下 |
| 空修正者的后续通知 | 低：仍先取得监听表，保留物化与通用来源异常；空列表不再创建通知context或遍历。非空路径保留原序成员查询 | 原型及语义对照完成，整搜对照见下 |
| 保命遗物查找临时分配 | 低：逐索引扫描分支遗物，保持遇到的首个未使用蜥蜴尾巴；不缓存其可变使用状态 | 原型及已使用/未使用合同完成，整搜对照见下 |
| Ritsu默认来源的线程内单条缓存 | 低至中：仍必须每次核对实例capability和登记generation；计数模拟最近Type/generation命中，仅27.62% | 未接入生产；收益未建立，不能把CanSkip的CPU占比当作全部可省成本 |
| 跨快照卡牌费用缓存 | 高：原生费用/可打出钩子约占数个百分点，但存在动态Power、费用历史、虚getter与第三方贡献；需要证明整个上下文的失效条件 | 未实现；仅热点上限，不能证明实际ROI，不放宽语义门禁 |
| 缓存战斗结束判定/扩大状态共享 | 高：IsCombatEnding self约1.59%；死亡、召唤、停止结束Hook与分支写入均影响结果 | 未实现；此上限不足以支持扩大状态失效/所有权复杂度 |

计数原型`c72651ddd00c44069fc8f198efefc783` Passed，使用从VeryHigh派生的Custom固定10,000节点/Beam135，其余维度原值，允许原药水审计/窄Beam恢复；请求累计60,000节点、893,527转移。它只量化调用和分配来源，不用插桩时间宣称提速：

计数从进程启动累计，到结果捕获处读取，可能包含建局/结果准备期间进入这些入口的调用；不把调用数乘对象大小精确等同于worker分配节省。

- 4,084,741次ModifyHpLost全部返回空修正者列表；Before/After调用分别1,646,416/2,438,325。空List对象按本机.NET9 32B估算约130.712MB，实际总收益仍看无插桩对照。
- 933,279次AfterModifyingHpLost通知全部为空，仍遍历15,588,240个监听位置。
- 845,317次保命遗物查询读取20件遗物，未找到蜥蜴尾巴；非空与已使用分支另由合同覆盖。
- 294,882,670次Ritsu默认Type缓存访问，最近Type/generation命中81,443,360次。它只估计单条缓存资格，不是速度测试；更大的线程缓存未实现。

## 语义验证

`2e0e2b78e1594761a73e12ef6d8260f5` Passed。新合同192组对照（168空/24非空），涵盖零/单层/多层Buffer、Intangible、小数截断、空/Before/After/双阶段、过滤Buffer、重复成员通知一次、移除后重获的实例身份；对照完整ContinuationStateText、RNG、父分支及live不变。参考管线冻结原四阶段与急切List收集，原生默认Hook按完整来源枚举，以独立于生产位图的路径对账；不声称覆盖未知第三方回调。蜥蜴尾巴未使用时复活预测、已消耗时旁路及源状态不变通过。

第一次候选构建因测试遗漏HpLossHookPhase的命名空间失败；补齐引用后构建0警告/错误，未进入失败测试，不隐去该记录。

## 无插桩对照

A–B–B–A在四个新进程运行，使用相同Custom固定10,000节点配置（VeryHigh的其他维度不变）、DOP16与NoGC16GB。每次请求累计60,000节点、893,527转移；正常药水审计及窄Beam恢复未删。诊断构建只在结果计量冻结后写完整结果/动作，不插桩搜索，不把这组Custom结果称为完整VeryHigh。

| 样本 | 搜索秒 | 分配GB | GC累计暂停ms | 峰值RSS GB | runId |
|---|---:|---:|---:|---:|---|
| A1 | 44.4683 | 48.681192 | 1394.228 | 22.458 | `a1e989b341ab4d77a517d9c8885858dd` |
| B1 | 44.4644 | 48.408058 | 2864.087 | 19.791 | `3d7d6962389e49a6b25b3dd908808970` |
| B2 | 44.3975 | 48.390608 | 1422.881 | 21.972 | `5e17c186e1f849e1bf21da6ca7c41c62` |
| A2 | 44.8816 | 48.675222 | 1410.277 | 22.391 | `2db8861669974042a7dfb3280068f654` |

四次均Passed，84项非时序字段、48步完整动作（含实例/选择/药水/目标）及其余结果文本全部相同。与分配或派发相关的运行时字段另列；没有只核对聚合HP。

平均分配48.678207252→48.399332696GB，减少278,874,556B（0.572894%），两对均下降。平均耗时44.674937→44.430957秒（−0.546122%），小于A1→A2约0.929%的基线漂移；第一对仅差0.004秒，不构成稳定提速证据。GC平均1402.2525→2143.484ms（+52.86%），完整保留B1的较大暂停，不扣除GC重算所谓纯搜索加速。RSS受冷进程堆分区/回收时点影响，只列原始样本，不宣称稳定内存峰值下降。

三项改动合并后的收益只按上述整组报告；调用计数和容器大小估算不是三个可独立相加的整搜提速数字。按用户“低成本且有实测收益可以实施”的要求保留该组分配优化，不宣称延迟或GC改善。


## 最终VeryHigh压力验证

最终普通生产构建（无计数器/结果dump/主搜索提前退出）使用完整VeryHigh：Beam135、100,000节点、300,000ms软预算、分支72/42/54，DOP16、NoGC16GB；不设FixedBudget或节点/时间覆盖。每场独立新进程、首个请求结果处停止，保留原主搜索/审计/恢复。按本批120秒无人请求期限运行，超时后不扩大等待重跑。

| 场景 | 状态 | 搜索秒 | 累计分配GB | 峰值RSS GB | 结果 |
|---|---|---:|---:|---:|---|
| 永世沙漏 / 死灵药水组合 | Passed | 77.198 | 93.423 | 29.343 | 预测T12胜利，战损2，用药2 |
| 机甲骑士 / 2305张极端牌堆 | 120秒超时 | — | — | 15.421 | 没有最终结果 |
| 女王 / 生成与选牌 | 120秒超时 | — | — | 23.528 | 没有最终结果 |
| 灵魂枢纽 / 30张牌 | Passed | 17.808 | 18.227 | 12.198 | 预测T9胜利，战损6，用药0 |

本轮2项Passed、2项未完成。Passed只证明请求及所列断言完成，战损与胜利仍是预测结果；没有做这四场的完整原生部署或逐动作差分。不同上游/预算/采样条件的历史记录不能组成优化前后速度倍率。

GB为十进制，RSS每100ms采样可能漏尖峰；NoGC16GB不是RSS硬上限。空白不是零，超时缺失完整搜索指标，不用部分子搜索拼出总量。总启动器墙钟包含建局/启动/停止，不当作搜索耗时。最终engine_risk等未落盘文本不补造，完整result及completedChecks保留于JSON。

- `781e12d50c544c229440a9cefed58d4c` / 永世沙漏 / 死灵药水组合：Passed；启动器墙钟111.368秒。累计展开/转移/选择145,597/1,705,319/1,048,545；GC累计/观测最大暂停1555.099/1477.853ms。
- `0571cf2d6fbd44dc8c67ed2a5e8ef4e5` / 机甲骑士 / 2305张极端牌堆：LauncherTimeout；启动器墙钟131.525秒。启动器明确记录期限超时并停止进程；未取得result.json。
- `b22552db2fb14ee3a893acf884e7df0b` / 女王 / 生成与选牌：LauncherTimeout；启动器墙钟131.829秒。启动器明确记录期限超时并停止进程；未取得result.json。
- `b0609ac8477e467bab60d41f902c7c47` / 灵魂枢纽 / 30张牌：Passed；启动器墙钟51.506秒。累计展开/转移/选择50,487/400,920/200,467；GC累计/观测最大暂停1675.839/1663.884ms。

本轮结论是三个小范围分配优化已通过边界对照与固定工作量验证；稳定提速、GC改善及全部极高负载通过均未建立。没有修改搜索政策、预算、版本或Windows部署。


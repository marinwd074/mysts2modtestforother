# 按真实 CPU 热点优化路由聚合（2026-09-08）

[性能索引](README.md) · [全部请求、比较与复跑参数](backend-hotspot-optimization-20260908.json)

保留 `BeamRetentionPolicy.RankBest` 的局部聚合优化：原来同一路由签名在六张字典中重复哈希、查找；现在一次查找取得有序候选列表及五项代表。基线为 `9a5860b`，极高参数、候选、评分、配额、状态键、模拟后端不变。按用户要求只做 headless A/B，没有可见性能结论。

## 热点依据与实现

重新细分[上一轮实际 CPU 样本](backend-cpu-hotspots-20260908.md)，排除回放、Fork 和 Snapshot 后，`RankBest` 路径占 816 / 23,915 个搜索样本（3.41%）；其中叶子字符串哈希 118、路由父链查询 115、Beam 评分 48、字典取值 32。这里位于串行保路阶段，多次散落查表值得合并。样本数不是墙钟时间，也没有以本轮耗时倒推 CPU 百分比。

`RoutingChoiceNodes` 继承原来的有序列表，只增加五个代表引用。首次成员初始化代表；后续成员照原比较与同分规则更新。候选添加、族/选项排序、Pareto、配额及最终仲裁保持原入口与顺序。scratch 仍由 policy 租借和归还，清空唯一字典即释放各组及节点引用，不池化节点组、不跨调用缓存结果；不新增分支模型状态。

## 保留版本的测量

两版每种配置均用独立进程，先做一次短搜预热，再做一次正式请求；固定同根、VeryHigh、DOP8、NoGC16GB、详细诊断关闭、首结果停止、120秒请求期限。正常配置允许原预设进入 Deep；药水政策沿用夹具的 `RequireAtLeastOne`，不代表全部玩家默认设置。

| 指标 | 基线 | 最终候选 |
|---|---:|---:|
| 正式短搜耗时 | 5.8670秒 | 5.6931秒（−2.96%） |
| 正常完整搜索耗时 | 41.1816秒 | 40.6614秒（−1.26%） |
| 正常搜索累计分配 | 35.3729GB | 35.3763GB（+0.010%） |
| 正常请求采样峰值RSS | 16.6027GB | 13.4046GB |
| 搜索观测累计GC暂停 | 1,969.532ms | 3,660.930ms |

短搜两版同 10,000 展开 / 144,368 转移 / 101,808 选择；正常配置同 46,239 / 636,428 / 431,140。正常搜索比较 90 项非耗时、非并发调度日志字段，全部一致；64 条完整预测 ACTION 行（含动作、目标、选择）逐行一致。仍为 Deep/None、投影战损9、仅死亡路线，Passed不表示获胜。

正常基线 `0c7c5b59ab2542ffa06ca40680856281`，最终 `5a64a1d297cf4dcb8060aa69a0ded3cc`。只有一组正式样本，最终 GC 暂停反而多约1.69秒。不能把耗时减GC当作CPU时间，也不能把峰值差异当成稳定节省内存；本轮只得到小幅提速证据，没有证明显著或成倍提速。

## 撤回的实验

以下均先独立构建、独立进程短搜预热后测量；不把冷启动时间列作收益。各实验源自相同基线，额外路由缓存叠加在保留的聚合版本上。

| 原型 | 正式短搜秒 | 处理 |
|---|---:|---|
| Power监听前缀边界 | 5.8465 | 收益不足，撤回 |
| 指纹小方法内联 | 5.8294 | 收益不足，撤回 |
| 原版关键词查询过滤 | 5.8681 | 短搜无收益，撤回 |
| Power插入边界 | 5.8665 | 无收益，撤回 |
| 牌列表Span填充 | 5.8691 | 无收益，撤回 |
| 共享监听尾部视图 | 5.9117 | 耗时回退，撤回 |
| 无相关附着效果的牌绕过查询 | 5.8668 | 无收益，撤回 |
| 路由签名预计算哈希 | 5.9394 | 耗时回退，撤回 |
| 聚合再加父链/评分缓存 | 5.7559 | 完整请求出现内部指标差异，撤回缓存 |

关键词原型的正常搜索为39.8507秒，但GC暂停从1,969.532降到73.945ms，足以解释全部表面收益；没有保留扩大语义面的这项修改。额外路由缓存正常搜索39.6309秒，展开/转移/选择及64条路线一致，但4项 deferred-round 诊断指标不同（83→70、22→25、1407→995、83→70）。本轮未归因差异来自调度还是缓存，不能视为完整指标等价，故撤回；不是已证实的战斗语义错误。

## 最终验证

- `SearchPolicySnapshot`：`9eb710f1f0e44877aa4fd3bdae084ee7` Passed；250节点DOP1/DOP2动作、评分、续用与非时序工作/剪枝一致，实际并发至少2，取消记账与冻结策略合同通过。
- 完整机甲骑士：`1e8dec2b7eda4d83b2b851ddd7babd5e` Passed；Instant/0秒、战损8、57HP、第7回合结束、不用药、零计划外重算。27条实际部署动作与上一轮 `f272c7ba66994d93b811a9001237a709` 逐行一致。该对照用于质量，不用于计时。
- 最终源代码 Release 构建零警告/错误（8.52秒），Bash/PowerShell 结构门禁均返回 `REFACTOR_BOUNDARIES_OK search_files=78`。正式搜索和合同都显式加载已固定的聚合版本 artifact；撤回缓存后最终构建刷新本地DLL，不替换游戏安装。

完整非时序搜索指标、逐行预测与实际动作比较覆盖此轮目标；没有宣称全卡语义或任意根状态等价。未跑增量、可见Steam、Windows游戏及发布门禁。无版本提升、ZIP或远端推送。

## 复跑

结构化记录 `commands` 提供基线/候选短搜、正常配置、策略合同及整场哨兵的完整参数数组，已将个人路径替换为仓库夹具路径或显式占位符。机甲骑士的临时牌组从 `coverage/unattended/performance-veryhigh-mecha-native.json` 的 `runCards` 提取。构建目录必须固定版本；每版独立进程先短搜预热再正式测量，结束后用 `--stop-instance` 退出，勿让旧DLL跨构建复用。

# 最终代码的 VeryHigh 极高负载测试（2026-09-13）

[性能索引](README.md) · [结构化结果与复跑参数](veryhigh-final-20260913.json)

用户明确要求在优化完成后补跑极高负载样例。本轮使用 `e528749` 的正常生产 Release DLL，包含五候选最终保留的改动；没有 primary-only 提前返回、MVID 绕过或性能探针。复用上一轮成功构建的产物，无生产源码变更。

## 配置与范围

- 当前 `VeryHigh`：Beam135、100,000节点、300,000ms软搜索预算、动作分支72、牌堆选择42、手牌选择54。未使用 `forceShortSearchOnly`、`FixedBudget` 或时间/节点覆盖；正常主搜索、药水审计和无胜利恢复均保留。
- DOP16、NoGC16GB、关闭详细诊断与增量验证。每场独立新进程，Linux headless，首个请求级 solver 结果处停止。未启动 Windows 可见会话或修改 Windows 部署。
- 仓库规定单个无人请求期限120秒。软搜索预算300秒不是本轮可等待的硬时限；超时记为 LauncherTimeout，保留缺失指标，不将启动器连同冷启动/退出的墙钟时间当作搜索耗时。未在超时后扩大等待期限重跑。
- 使用历史压力报告的同一固定输入：死灵契约师药水组合、2305张极端牌堆、女王生成选牌，以及灵魂枢纽30张牌。前三项是明确注入的合成根；药水组合是白名单投影重建，不是存档恢复。灵魂枢纽保留原生敌人生命、行动与开局效果。
- RSS每100ms采样，覆盖独立进程冷启动至请求结束/停止，可能漏掉短尖峰；累计分配不等于同时占用。NoGC16GB不是进程RSS硬上限。GC暂停为请求观测值，不是可见帧时间或独立trace全局最大值。
- 每场只取一次本轮样本，不做优化前后性能比较。旧报告使用不同版本、DOP和旧Short/Deep预算，不能据此计算提速倍数；Passed仅表示请求和断言通过，不能当作原生整场战斗获胜或状态等价证明。

## 本轮结果

| 场景 | 请求状态 | 搜索秒 | 累计分配GB | 峰值RSS GB | 结果 |
|---|---|---:|---:|---:|---|
| 永世沙漏 / 死灵药水组合 | 超时 | — | — | 33.592 | 未取得最终结果 |
| 机甲骑士 / 2305张极端牌堆 | 超时 | — | — | 15.276 | 未取得最终结果 |
| 女王 / 生成与选牌 | 超时 | — | — | 28.550 | 未取得最终结果 |
| 灵魂枢纽 / 30张牌 | Passed | 7.968 | 6.474 | 8.149 | 预测胜利，战损6，T9 |

GB均为十进制。超时行的空缺不是零；不以未完成子搜索或部分日志拼出整次请求指标。

### 永世沙漏 / 死灵药水组合

runId：`fb93442e679349fc8bcb9f2f1e8dd2eb`。状态：`LauncherTimeout`；启动器总墙钟（含冷启动和退出）134.213秒。

启动器明确记录120秒期限超时并停止游戏；退出码1，没有`result.json`。已落盘内存检查点13条；这是可观测日志数，不当作完整回收次数。

### 机甲骑士 / 2305张极端牌堆

runId：`5139fd4ce4eb4b1295ca81ef49d69eb8`。状态：`LauncherTimeout`；启动器总墙钟（含冷启动和退出）131.623秒。

启动器明确记录120秒期限超时并停止游戏；退出码1，没有`result.json`。已落盘内存检查点0条；这是可观测日志数，不当作完整回收次数。

### 女王 / 生成与选牌

runId：`1278120c555846b695fe8f1e646a1955`。状态：`LauncherTimeout`；启动器总墙钟（含冷启动和退出）131.632秒。

启动器明确记录120秒期限超时并停止游戏；退出码1，没有`result.json`。已落盘内存检查点7条；这是可观测日志数，不当作完整回收次数。

### 灵魂枢纽 / 30张牌

runId：`be412b0035104e85823b0363ad4e0704`。状态：`Passed`；启动器总墙钟（含冷启动和退出）41.069秒。

请求累计展开/转移/选择：16,897 / 141,888 / 78,511。最终边界`None`，搜索回合9，洗牌3，用药0。

GC累计暂停0ms，观测最大暂停0ms；Gen0/1/2次数0/0/0。NoGC生命周期：`{"forcedCollections": 0, "noGcStartAttempts": 1, "noGcStarts": 1, "noGcEndAttempts": 0, "noGcEnds": 0, "noGcRestarts": 0, "noGcLosses": 0}`。最大并发16。

## 结论与未取得证据

四项中1项Passed、3项LauncherTimeout。灵魂枢纽通过请求断言，完成16,897展开、141,888转移、78,511选择，预测T9获胜、战损6、零用药，观测GC暂停为零。另三项无法给出最终路线或完整搜索计量，不能声称本轮极高负载全部通过。

灵魂枢纽的completedChecks明确验证了VeryHigh/300000ms/Beam135/100000节点/72分支与NoGC16GB，且记录Unmirrored=0；最终engine_risk与modeled_damage_exact文本未落盘，不补造这些字段。所有完整result字段和completedChecks保留于结构化记录。极端牌堆进程的会话日志为空，不能从其RSS反推搜索阶段或转移数。未追加构建、行为回归或超时重跑；本轮改动仅为证据文档。


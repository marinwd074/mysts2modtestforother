# 回合结束探针与元数据热路径优化（2026-09-09）

[返回性能目录](README.md) · [结构化证据](standpat-and-metadata-20260909.json)

本轮以 `11d278a` 为独立基线，正常极高配置的第二组八次交错测量得到 **37.6927→25.4926 秒，平均耗时减少 32.37%**，平均分配减少 17.14%。不累计上一轮的 10.93% 收益。第一组仅减少 24.74%，完整保留在下文；探索单样本不用于达标结论。

## 固定条件与比较口径

使用既有 NECROBINDER / AEONGLASS_BOSS 药水压力输入：38 张跑局牌、19 件额外遗物、两瓶药水及固定开局。只测试 VeryHigh，DOP8，NoGC 配置上限 16 GB，详细诊断与增量回放关闭。搜索预算保持 10,000/50,000 节点、Beam 54/135、动作分支 45/72；每个 unattended 请求总期限 120 秒，首个结果停止。

最终计划在测量前固定为 **B1–C1–C2–B2–B3–C3–C4–B4**。每个条目启动独立进程，用同一冻结构建做一次 Short 预热，再测一次正常配置，随后停止实例。记录所有八个样本，不丢弃慢样本；比较算术均值、范围、首尾漂移、分配、GC 与每 100 ms 采样的进程 RSS 峰值。启动/资源租约等待不进入 solver 耗时，RSS 包含游戏和预热残留。GB 均为十进制。

B4 第一次尝试在进程启动前因主机独占资源排队超时，未产生预热或正式搜索结果；失败启动记录单独保留。该位置等待主机空闲后使用同一冻结构建和原参数补齐，不延长请求期限，也不替换此前七个正式样本。

第一组逐项比较 `RESULT` 中 83 项字段，第二组扩展到 90 项（加入全部七项 deferred-round 诊断），均逐条比较 64 条完整预测 `ACTION`（含目标与选择）。第二组发现其中 `phase / deep_triggered` 实为耗时派生标签，下文保留原比较并说明分类修正；其余 88 项非时序字段全部相同。所有正常请求均为 46,239 展开、636,428 转移、431,140 选择分支，投影战损 9，仍只有死亡路线。字段与所选路线相同是本 fixture 的非退化证据，不等于逐转移严格状态差分或新的胜利证明。

用户此前明确暂不测可见会话，因此本轮口径限定为 headless；不能据此宣称可见 Steam、全部 Mod 组合或所有遭遇均获得相同比例的提速。配置 16 GB 是上限，Runtime 仍按系统余量缩小实际 NoGC 区域，实际区域大小与回收次数单独记录。

## 第一组交错测量：未达到目标

| 顺序 | 来源 | 耗时（秒） | 分配（GB） | RSS 峰值（GB） | 搜索内回收/重启 |
|---|---|---:|---:|---:|---:|
| 1 | B1 | 37.3618 | 35.6636 | 17.4697 | 3 |
| 2 | C1 | 28.3751 | 31.5807 | 17.2247 | 3 |
| 3 | C2 | 28.0251 | 31.5768 | 17.3553 | 3 |
| 4 | B2 | 37.3845 | 35.6903 | 20.5619 | 3 |
| 5 | B3 | 37.3943 | 35.6653 | 17.0306 | 3 |
| 6 | C3 | 26.9143 | 31.5682 | 17.0396 | 2 |
| 7 | C4 | 29.5711 | 31.5833 | 17.1442 | 3 |
| 8 | B4 | 37.8501 | 35.6934 | 17.6243 | 3 |

每版四个样本。基线范围 37.3618–37.8501 秒，候选范围 26.9143–29.5711 秒；首尾漂移分别 1.31% / 4.22%。平均分配 35.6782→31.5773 GB（−11.49%），平均采样峰值 RSS 18.1716→17.1910 GB（−5.40%）。所有正式样本均通过，83 项字段与 64 条预测动作一致。最后一个基线在上述启动失败后原位补齐，没有删减或替换已测样本。

50 ms 线程调度采样得到的搜索相关线程组平均用核约 3.65→4.56。该组为 lane 与 ThreadPool 之和，包含其间的运行时工作，可能漏计采样间退出的线程，不是精确纯搜索 CPU；详细时间线单列 GC/其他线程与 runqueue 等待。候选三次回收的请求为 28.03–29.57 秒，两次回收的一次为 26.91 秒。实际 NoGC 上限随系统余量收缩，累计分配减少尚不足以在所有样本中省掉第三次回收。

该组等工作量吞吐提高 32.87%，但本轮事先承诺的验收标准是**耗时减少 30%**，因此不把吞吐数字用作达标结论。

## 第二组交错测量：平均耗时减少 32.37%

第一组之后增加监听分段、有效/活动前段复用、组内排名摘要和两处枚举比较优化。再次使用原定 B1–C1–C2–B2–B3–C3–C4–B4，每个版本四个新进程，冻结构建后没有修改生产源码。八个条目均按序完成，没有替换或丢弃样本。

| 顺序 | 来源 | 耗时（秒） | 分配（GB） | RSS 峰值（GB） | 搜索内回收/重启 | 最大观测 GC 暂停（ms） |
|---|---|---:|---:|---:|---:|---:|
| 1 | B1 | 37.8515 | 35.6951 | 19.9248 | 3 | 28.600 |
| 2 | C1 | 25.9025 | 29.5683 | 16.8093 | 2 | 2079.399 |
| 3 | C2 | 24.9764 | 29.5592 | 15.0564 | 2 | 1753.381 |
| 4 | B2 | 37.5267 | 35.6716 | 19.2521 | 3 | 27.744 |
| 5 | B3 | 37.5996 | 35.6628 | 19.5917 | 3 | 26.585 |
| 6 | C3 | 26.1162 | 29.5735 | 16.8306 | 2 | 2096.870 |
| 7 | C4 | 24.9752 | 29.5654 | 16.9706 | 2 | 29.293 |
| 8 | B4 | 37.7928 | 35.6975 | 19.4972 | 3 | 30.510 |

每版四个样本，基线范围 37.5267–37.8515 秒，候选范围 24.9752–26.1162 秒；首尾漂移 −0.16% / −3.58%。平均分配 35.6818→29.5666 GB（−17.14%），平均采样峰值 RSS 19.5665→16.4167 GB（−16.10%）。50 ms 调度时间线中的搜索相关线程组平均用核 3.66→4.59，累计 CPU 138.06→117.27 秒；线程组定义与采样局限同上，不能称为精确纯搜索 CPU。

本组最长暂停没有改善：基线最大观测 GC 暂停为 26.6–30.5 ms，候选为 29.3–2096.9 ms，三次候选仍有长暂停。虽然配置均为 16 GB，最终实际 NoGC 区域由既有 Runtime 根据内存余量缩小，基线四次均约 13.5188 GB，候选为 16/16/16/14.7764 GB。没有修改该政策或预算。平均耗时结论包含这些暂停，不能据此声称消除了卡顿或达到可见会话的帧延迟目标。

C2 暴露了原比较字段分类的一项错误：其主搜索在 19.973 秒完成，`phase=Short / deep_triggered=False`，但正常 Deep 预算和窄 Beam 恢复均已完整执行。`CombatSearchCoordinator` 在基线与候选中完全相同：正常搜索一开始就采用 Deep profile，两个输出标签仅按单次 solver 是否跨过 20 秒检查点赋值。原始 90 字段比较及其失败状态保留；源码审计只将这两个字段重新归为耗时派生量，其余 88 项与 64 条动作逐项一致。八条命令的实际 profile/预算、主搜索 34,239 展开、恢复后 46,239 展开及完整转移均核对一致。没有修改样本、预算或 30% 耗时门槛，也没有改生产代码来统一标签。结构化证据同时保留原比较和分类修正。

## 成本定位与保留实现

基线 Linux perf 使用 `cpu-clock:u`、199 Hz、frame-pointer 调用链和 JIT 符号，得到 28,112 个已解析搜索样本。互斥分组为回放 46.09%、快照 28.35%、Fork 12.30%、其他搜索 9.43%、回放以外的保路 3.82%。其中 coordinator 上的 `EvaluateStandPat` 有 1,381 个样本：旧分类把这部分串行 EndTurn 模拟计入回放，不能只凭“保路 3.82%”推断 coordinator 没有优化空间。inclusive 项相互重叠，不相加，也不把阶段 Stopwatch 时间称为 CPU 时间。

- **EndTurn 探针共用固定 lane。** 原来保路逐项串行执行完整回放；现在只对原规则必经的未缓存探针批量求值。按原 `StateKey` 的首个代表去重，worker 只回传四项标量，临时模拟器在 `finally` 释放，coordinator 原序写缓存和挑选代表。仍使用完整原版回放与快照，没有新节点准入、选择预算或批内回收点。取消/异常先停止派发、排空并记账，再传播原 token/异常；DOP1 保留串行路径。
- **规范药水代价值按单次 solver 复用。** 原保路与终局排序反复遍历整个药水目录；小表按 ID/可再生条件缓存原查询结果。未命中仍走原 `Single`，缺失或重复 ID 继续失败。不存分支药水实例，不把实例可变属性当作规范目录值。
- **复用精确目标类型的缺失证据。** 只有 Ritsu 原全程序集查询成功返回 null 后才能跳过后续解析；校验 AssemblyLoad 代次，并逐次查询弱引用中的动态程序集。新程序集、动态晚建类型、正结果和异常均保留原路径，live 调用旁路。此缓存不是框架安装状态或目标合法性缓存。
- **原生空关键字回调旁路。** 根捕获同时审计基础回调和原生 `Hook.ModifyKeywordsInCombat` 的补丁；确认全部接收者均为默认空操作时，直接查询牌的局部关键字。外部/动态类型、不透明 CardModifier 与补丁仍保留原调用。
- **同根共享不可变监听类型布局。** 2048 个直接映射槽，单布局最多 1024 项；哈希只选槽，完整类型顺序精确匹配后才复用。布局仅含 Type/位图，不含 Model，当前分支继续提供接收者；碰撞和并发替换只导致未命中。最大条目负载约 32 MiB，另有数组/对象开销，随根释放。命中、碰撞与旁路为根累计计数，主搜/恢复数不能相加。
- **原生监听分段与派生前段复用。** 基础前段仅包含阵容、遗物、药水与根 Power；卡牌/球及原生附着效果在后段。成功分段后，卡牌/球变化保留有效/活动前段和 Power 投影；Power、阵容、药水变化仍完整清空。新增 Power 缺少前段锚点时保留原完整列表插入位置；不透明 CardModifier 保留完整列表及追加器上下文。各段不可变，Fork 通过同一上下文重映射，包括完整后段已失效的前段。最新单次正常搜索有效前段复用 576,163 次、构造 622,829 次；该指标为根累计，不能再加主搜索日志。
- **组内排名摘要。** 每个 `RankBest` 组填满后用原 `Max/Min` 冻结最高 Beam 分、最高父分和最低父排名，只在原排名赋值前消费，下一次调用重建。没有复用历史父排名或改变 NaN 规则；比对额外纳入全部七项 deferred-round 指标。
- **同枚举比较去装箱。** 原生 `PowerModel.GetTypeForAmount` 两处 `Equals` 只有在指令形状、枚举底层类型、标签与异常区域均满足条件时才替换为整数比较。虚 getter 与 decimal 分支的调用顺序和次数不变，未知 IL 原样保留；不缓存 Power 类型或层数。
- **局部重复工作。** 同一只读快照的 Power 贡献判定只算一次；引用成员检查按索引遍历，避免接口枚举器装箱；路由父链遇到早于原选择窗口的动作便停止，但保留前一回合的 EndTurn，避免漏掉其携带的下一回合选牌。

新的完整分配采样用于最后三项选择：324,284 个 AllocationTick，估算 34.8599 GB，其中已确认搜索 34.7116 GB，丢失事件/缺失调用栈均为 0。监听布局 `Entry[]` 约 2.1300 GB；Power 贡献查询的两种枚举装箱合计约 0.7733 GB；引用成员检查的装箱枚举器约 0.1317 GB。以上为采样估算，不是精确逐类型字节数。分析器补齐 `AdmittedExpansionJob.Execute` 与 `StandPatJob.Execute` 搜索锚点，避免把作业内分配误归为 lane 基础设施。该 trace 来自布局/贡献判定优化之前，不当作最终版分配分布。

第一组之后的 CPU 采样共 27,038 条，其中搜索调用栈 23,055 条，缺失栈 0、达到 127 帧上限 11；原始 trace 的 JIT 导出失败已修复并重新解析，未重跑搜索。互斥分组为回放 41.05%、快照 30.04%、Fork 14.69%、其他 11.02%、回放外保路 3.21%，仍有未解析 CLR 原生符号。有效监听读取从先前 2,200 条降到 1,167 条；这是不同采样窗的定位证据，不能直接相减为墙钟收益。

最后三个正常单样本为 26.4392/26.5061/26.4646 秒，线程时间线中的 lane CPU 为 108.79/109.87/107.10 秒，lane 调度排队仅约 1 秒。线程组统计包含运行时工作，不把锁等待或 GC 归成纯搜索 CPU。最后一次仍有两次回收，其中一次观测暂停约 2.12 秒；这些数据说明必须用正式交错对照处理回收时点与系统余量的波动。

## 语义与所有权验证

- 最终冻结候选 `8effcf7a2e804c52867335cf32e636aa`：`SearchPolicySnapshot` Passed，请求 56.99 秒。Deep 1000 节点合同实际执行 641 次探针，两个 lane 在同一屏障相遇；取消/异常注入后无在途回调，原 token/异常身份保留，部分工作只记录一次，随后原根可复用，DOP1/DOP2 的完整结果、动作/选择、评分、续用及非时序工作/剪枝相同。
- 第一组布局行为 `1424338c0a8e4ade8429d4611543f766` Passed：55 项回调、1670 个 Model，完整类型顺序、重复接收者、外部类型、Fork 身份、强制哈希碰撞和并发替换。关键字另与原生 Hook 集合逐项比较，覆盖 Hex/移除、分叉隔离、局部关键字和原生 Hook 补丁刷新。其后第一组最终产物只增加目标类型缓存累计日志，不重跑未变化的该边界。
- `RitsuTargetTypeLookupChecks` 23 项通过，包括原生产回调、静态/动态类型、晚加载/晚创建、失败不发布证据、live 旁路和可卸载程序集弱所有权。
- 第二组候选 `5d194a22bcc8496c998ba396f9873e83`：55 回调/1,670 Model 与 ForkBoundaries Passed，覆盖有效前段跨卡牌变化复用、子分支 Power 投影身份、无锚点完整路径和完整失效。最终源码 Release 构建 4.07 秒，零警告/错误；其后仅修改文档与本地分析脚本。
- `PowerAmountComparisonChecks` 8 项通过：1,568 组输入核对原生输出与 getter 顺序（含变化 getter、未定义枚举值和 decimal 边界），未知/内部跳转 IL 旁路；10 万次原生调用的装箱分配 9,600,000→0 字节。
- 独立 Knights Short 哨兵 `9664c01a18084c92afe111f9d47ce7e8` Passed，与基线 `a4683db7ba964e259533372424f94250` 的全部 90 项字段和 9 条完整预测动作一致。3,448 展开、8,796 转移，仍为死亡路线；不把此小场景当作新增胜利或整场部署证明。
- 正常 Deep profile、1 GB NoGC 对照 `591767d49821417d90d7134ce000d764` / `2345c8785fac4c8fb6d9b81e72aa43ae` 均 Passed，全部 90 项字段与 64 条动作一致。主搜索及恢复合计仍为 46,239 展开、636,428 转移、431,140 选择，期间基线/候选经历 63/52 次回收与重启，无 NoGC 丢失。搜索耗时 56.9968/42.4920 秒，分配 36.1244/29.7992 GB，请求总时长 81.80/67.33 秒；每个请求均未超过 120 秒。此单组用于低预算边界验证，不替代上述 16 GB 配置的八样本平均耗时结论。

Bash 与 PowerShell 结构门禁均通过（`REFACTOR_BOUNDARIES_OK search_files=82`）。没有本轮整场原生部署、逐转移增量搜索、Windows 游戏或可见 Steam 结果。

## 撤回的探索

监听 Power 前缀、单独保留 Power 投影、无附着效果旁路及 `RankBest` 直接优化 JIT 均未建立明显速度收益，全部撤回。对应单样本正常耗时为 32.8008、28.8672、29.0679、29.2620 秒，不能将小于已知漂移的差异解释为确定回退或收益。保留版本的探索耗时依次为探针 33.0197、药水表 30.3219、类型缺失证据 29.0633、关键字 28.8622、路由窗口 28.4372、单次贡献判定 28.9791、512 槽布局 28.1450、引用扫描 27.5953、2048 槽布局 26.2065 秒。单因素只是筛查，组合收益以最终交错测量为准，不相加。

后续单因素探索为监听分段 27.9030 秒、冻结拼接长度 26.4470 秒、排名摘要 26.4520 秒、枚举比较 26.4392 秒、有效前段复用 26.4646 秒。最后一项较枚举版减少约 339 MB 分配，尚未单独建立耗时收益。

Ritsu 默认能力来源的线程末次类型缓存已撤回：合同 `3e205a3ac0c542c296d8ebef87023a30` 通过，运行中注册、实例能力变化与线程隔离正确；正常 `a6b3478830614d33a08c0591b43255f5` 为 26.5061 秒，90 字段/64 动作一致。分批计数命中约 24.7%，额外未命中路径没有带来可辨认的速度收益。保留报告与补丁，不保留该实现。

## 复跑

使用[压力输入固定命令](veryhigh-pressure-survey-20260908.md#复跑示例)，每次显式指定冻结的 DLL/manifest 目录；预热保留 `--force-short-search-only`，正常测量移除该项，保持上述八条目顺序。1 GB 边界仅将 NoGC 配置改为 1，仍运行正常 Deep。

深搜探针合同使用相同建局，场景名必须是 `STAND-PAT-PROBE-BATCHES`，添加 `--verify-search-policy-snapshot --verify-fork-boundaries --stop-after-combat-root-snapshot-assertion`，移除首结果/强制短搜/NoGC 性能参数；总期限仍为 120 秒。布局合同使用 `MIRRORED-HOOK-FILTER`、IRONCLAD、FUZZY_WURM_CRAWLER_WEAK，首根断言后停止。两端现有脚本分别接受 GNU 长参数和对应 PascalCase 参数；没有新增平台专属生产入口。

原始日志、冻结构建、CPU 时间线、trace 和采集脚本保留在 `.local/perf30-20260909/`，不进入源码提交。

# 五项后续候选：工作量、原型与取舍（2026-09-13）

后续按用户要求使用最终生产构建补跑[完整VeryHigh极高负载样例](veryhigh-final-20260913.md)：1项Passed、3项120秒超时，不能把本页局部收益当作全部极高负载验收通过。

先把前六方向的最终保留代码`415da12`部署到用户Windows，再开展本轮。新研究保留小类型计数表、溢出字典单次查找，以及两个生成入口的已有根缓存复用；未采用deferred top-k和JIT/SIMD原型。**当前Windows仍是先行部署的415da12，本轮新增代码尚未再次部署。** 完整[原始证据](five-candidates-20260913.json)与[可重跑独立原型](../../tools/PerformanceCandidateProbes/README.md)一并保留。

## 1. 工作量评估与授权边界

以下是改动范围和维护成本的估计，不是工期承诺；低成本仅指实现及可控验证面，不能把微基准收益视为整搜收益。高成本且有明显实际收益的方案需先报告，等待用户决定后再写正式实现。本轮没有形成这样的高成本采用建议。

| 方向 | 成本判断与切口 | 原型/实测 | 决定 |
|---|---|---|---|
| deferred top-k | 简单堆低；严格保留旧不稳定排序的部分选择中高，需维护比较/划分、同分身份及运行时变化 | 普通堆1812项对照955项不同；15份真实输入中5份非平凡输入全有同分，全部改变输出；保守回退0差异但这5份均需全量排序 | 当前真实样本收益低，不写正式部分排序 |
| 小类型计数表 | 低：一个私有辅助容器，主state/别名及Fork回调不迁移；需溢出、归零、重入与分叉合同 | 1–3类每次计数表复制216→64B；3万节点整搜分配−0.361%，1万节点−0.251%；耗时收益未建立 | 保留分配削减 |
| 字典单次查找 | 很低：只在计数溢出表递增时立即消费ref | 500万更新约115.43→49.76ms，零算法分配；本场总递增仅13,219次，改为小表后常见路径不再使用该字典 | 保留窄入口，不重复计算整搜收益 |
| 额外生成池 | 两个既有池消费者低；通用跨类别/角色/额外谓词缓存较高，需更多资格与失效审核 | 两个原生生成入口完整状态/RNG合同通过；局部每1000次生成分配分别约8.60→2.63MB、8.49→3.03MB | 保留两个入口；广域迁移当前收益证据不足，不实施 |
| JIT/SIMD | 单属性/局部副本低；跨快照连续布局与批量向量化高 | 单纯内联提示差异低于漂移；两链SIMD约慢92%，字符串局部副本约慢28% | 三个局部原型均未采用；不改宿主全局配置，不扩展高成本布局重写 |

## 2. Windows先行部署

从Windows读取当前游戏与Ritsu依赖，构建`415da12`，Release零警告零错误（9.57秒）。SSH部署命令成功，目标`C:\Program Files (x86)\Steam\steamapps\common\Slay the Spire 2\mods\CombatSolver`；旧四文件备份到`%LOCALAPPDATA%\CombatSolver\deployment-backups\415da12-six-20260912-195708`。复制DLL、manifest、未变的MemoryCleaner和第三方NOTICE。部署时没有运行中的sts2，未启动可见游戏、改版本、发ZIP、标签或上传。之后只在Linux做本轮实验。

## 3. 当前热点与小类型表

基线`415da12`，真实BaseLib/Ritsu、Linux无头、DOP16、16GB、3万节点。诊断`b3413da9127145d489f68fead7c5cd74` Passed，85项质量/工作字段及22步完整路线与未插桩基线一致。线程内计时含等待、插桩与嵌套调用，不能当作纯CPU或墙钟比例。

| 子路径 | 次数 | 线程内经过ms | 累计分配B |
|---|---:|---:|---:|
| StateStore Fork | 419815 | 1208.018 | 301680792 |
| HasEntries | 2269940 | 222.153 | 0 |
| IncrementCount | 13219 | 5.908 | 162864 |
| Remove | 6 | 0.298 | 424 |

419,815次Fork的有效类型数为：0类20,849次、1类389,776次、2类9,190次；没有3类以上。于是保留空store原大小，只在有类型时创建三槽对象，第四种类型才建立原生Dictionary。新计数表不存Model或state；主表的插入/枚举顺序、factory重入、别名处理和实际state Fork仍沿原路径。父表零计数保留，Fork只复制非零项。溢出时多一个64B包装对象，不能宣称所有第三方组合的保留内存都下降。

独立原型821,632项随机增减/归零/Fork对照通过。链接实际StateStore源码的旧/新合同均通过，新增35,896项覆盖8种类型、4个模型、溢出、全清空后Fork、父子隔离，以及工厂重入插入8种状态后再完成外层状态。主表ref没有跨越工厂。局部计数表复制1–3类216→64B；4类累计464→392B，8/16类992→920B（累计扩容分配，不是保留体积）。空表不分配计数容器。

真实`STAND-PAT-MEMORY-BOUNDARY` / `8a251861ee034346b8f37788b7ba3aee` Passed，覆盖104MiB人工压力、取消/异常排空、同根复用与DOP1/DOP2完整结果相同。

### 3万节点正式对照

A为`primary-six-coverage`（生产源码与415da12相同），B仅含小计数表及其溢出ref递增；牌池入口后来单独开发，不计入本组。四个新进程A-B-B-A，真实BaseLib、DOP16、16GB、无插桩，固定首个主搜索。

| 样本 | runId | 搜索秒 | worker分配GB | GC暂停ms |
|---|---|---:|---:|---:|
| A1 | 8ea7955e53174f27865093df835135a0 | 23.3115 | 23.151258 | 1840.760 |
| B1 | fe43dc81c4a940ce9c99493f0a821e94 | 24.8309 | 23.065517 | 4166.077 |
| B2 | aeab622c58e94069830cf96324c151ec | 23.2804 | 23.046718 | 1930.477 |
| A2 | e5fdd29ef71f47a8a598b9442a615b86 | 23.2135 | 23.128118 | 1960.167 |

均值23.2625→24.0557秒（**耗时+3.410%**），两对分别慢约6.52%/0.29%；分配23.139688→23.056117GB（少83.571MB/**0.361%**），两对均下降。GC暂停均值1.900→3.048秒，B1有4.166秒暂停。未扣GC重算或删去B1，未把本组写成提速。

### 为排查运行开销追加的1万节点对照

在看到上述GC差异后，预先固定1万节点、同DOP16/16GB及A-B-B-A，检查不触发回收时是否还有明显回退；不是修改生产预算。四次GC暂停均为0。

| 样本 | runId | 搜索秒 | worker分配GB | GC暂停ms |
|---|---|---:|---:|---:|
| A1 | a49d9cf81829426bb0b3aeff383dbb08 | 6.8652 | 3.670141 | 0.000 |
| B1 | b4fbd340449f4948901bd5f1318b02db | 6.9435 | 3.661783 | 0.000 |
| B2 | 041fb46e4e854ba0b50e9a11a22711f6 | 6.9755 | 3.660394 | 0.000 |
| A2 | 35c02a2ae1fc495ebc718df3d76daaed | 7.0114 | 3.670439 | 0.000 |

均值6.9383→6.9595秒（+0.306%），两对一快一慢，低于A基线2.13%漂移；分配3.670290→3.661088GB（少9.202MB/0.251%），两对下降。**保留分配削减，未建立稳定耗时或GC改善。** 两组各自85项质量/总工作字段及22步完整路线一致，都是Passed/NodeLimit、最大并发16；不跨预算比较百分比。

## 4. deferred top-k：原型验证后不采用

普通主搜索没有开启`RecoverDeferredTurnFrontier`。第一次通过政策JSON要求开启时，请求本身Passed，但最终正常搜索profile未保留该标志，目标调用为0；它不是deferred验证。随后只在诊断产物的构造处强制开启，固定1000节点，`40bf353f9e7047359d5ebadfb4278182` Passed；该开关没有进入生产。

15次实际deferred排名，非空大小173、255、40、7、167，K=128，其余为空。排序/截取部分合计0.171482ms、10,552B，不含外部ToList及探针复制；整个诊断搜索576.832ms。全部5份非平凡输入存在同分。冻结输入及输出见[样本](experiments/deferred-inputs-20260913.json)。

简单优先队列在1812组测试中955组改变原输出，包含真实输入5组；测试含NaN、正负零、Infinity、不同K和大量同分。加完整比较键重复检查、遇同分回退原List.Sort后0差异，但本组真实输入无可用快路径。合成唯一键N=32768/K=128、300次的全排序/保守堆约1014.28/392.08ms；N=4096约88.97/77.61ms。大唯一池具有算法空间，**不代表当前失败恢复中出现了这种热点**。

要在这些同分输入上继续省排序，需要维护与现有运行时不稳定排序相同的划分、交换和输出身份，成本明显高于普通堆。本轮真实排序开销小，未实施该方案，也未把普通RankBest的完整保路/配额池替换为top-k。[运行时排序实现参考](https://raw.githubusercontent.com/dotnet/runtime/v9.0.9/src/libraries/System.Private.CoreLib/src/System/Collections/Generic/ArraySortHelper.cs)。

## 5. 额外生成入口：复用已有资格证明

当前诊断已有199,208次根无色缓存调用，角色攻击有放回缓存调用0；通用有放回入口489次、distinct入口2547次。后两者方法体计时与延迟枚举消费并非同一范围，不能据此相加为生成CPU。已有主路径缓存不算本轮新增。

本轮仅将`BundleOfJoyOnPlay`接入既有无色distinct helper，给`InfernalBladeOnPlay`增加使用既有角色攻击池的distinct helper。共用原根身份、原生/不可变模型、AllCards身份、玩家及约束门禁，未添加新的根缓存类别。**distinct仍使用TakeRandom，不因只取一张就换成有放回NextItem**；创建独占卡牌及所有随机数推进仍在当前分支。

合同`a25ce6aae00e4e729d1adada8edb11f3` Passed：16组新增完整对照，包含两种实际生产mirror、升级前后、三个RNG起点，以及distinct取0/1/3/128张。比较完整预测状态和RNG五字段、逐卡状态/独占性，父分支及live文本不变；复用现有根池合同检查顺序、共享只读候选、约束不匹配、mutable/custom池拒绝。新增helper的命中路径实际执行，回退保持原生成链。

首版微计量只写日志，证据未保留数值；没有据此报告时间。随后独立的仅计量请求`85f3392ba82d4a998c1c0225550a975b` Passed，将每份指标写入结果协议；未重跑已通过的16组语义比较。每样本1000次原生单张生成，包含独占卡牌创建：

| 候选池 | 样本 | 经过ms | 分配B |
|---|---|---:|---:|
| 无色（distinct） | A1 | 22.2734 | 8611344 |
| 无色（distinct） | B1 | 5.9020 | 2625784 |
| 无色（distinct） | B2 | 5.8903 | 2625784 |
| 无色（distinct） | A2 | 25.1738 | 8595872 |
| 角色攻击（distinct） | A1 | 21.3431 | 8493704 |
| 角色攻击（distinct） | B1 | 6.7352 | 3028352 |
| 角色攻击（distinct） | B2 | 6.3759 | 3026912 |
| 角色攻击（distinct） | A2 | 20.2440 | 8484352 |

无色分配均值8.604→2.626MB，角色攻击8.489→3.028MB。它们是有真实模型和缓存命中的局部结果，**不是整搜提速**；本轮没有为两个新增调用方建立整场收益。广域角色/技能/能力/额外谓词缓存还需更广的源对象资格及失效证明，而当前主搜已有缓存覆盖大多数已测调用，因此未擅自扩建。

## 6. JIT/SIMD：不采用三个原型

取得实际游戏Tier1汇编：`StateFingerprintBuilder.Add(ulong)`独立方法83B；字符串循环已经内联字符混合。单独方法存在不等于所有调用都付一次调用成本。纯探针使用.NET 9.0.19、FullOpts（仅探针设置`DOTNET_TieredCompilation=0`），**没有修改游戏宿主设置**。[宿主编译设置](https://learn.microsoft.com/en-us/dotnet/core/runtime-config/compilation)。

- 单独给ulong混合加AggressiveInlining：A1/I1/I2/A2为168.980/165.724/165.140/164.560ms；原实现与提示版的FullOpts循环都已内联，差异低于基线漂移，未采用提示属性。
- 两条独立混合链打包Vector128，乘法及分道移位：V1/V2为322.238/319.894ms，约慢92%。实际汇编已有AVX-512的vpmullq，打包/抽取与串行依赖仍使该形状更差；没有凭指令集更新就声称更快。
- 字符串先复制builder到局部、最后写回：A1/L1/L2/A2为308.270/389.660/393.130/305.324ms，约慢28%，撤回。

标量/内联/SIMD逐个前缀核对100,000次完整128位结果；字符串含null/空串等10,000个前缀一致。以上均零算法分配（每行48B为计量结果记录），结果/汇编摘要保留。跨多个快照重新组织连续数据需要更大的调度和布局变动，未证明本项目收益，未启动正式改写。

## 7. 最终验证与限制

生产Release零警告零错误；Bash与PowerShell结构门禁Passed，Search文件89个，PowerShell在Linux执行。独立原型项目可构建，未进入Mod运行时。保留原预算、评分、候选与完整路线，未改RNG、未知第三方回退或全局JIT设置。字典ref仅在局部立即消费，符合[CollectionsMarshal的引用有效期要求](https://learn.microsoft.com/en-us/dotnet/api/system.runtime.interopservices.collectionsmarshal.getvaluereforadddefault?view=net-9.0)。

这些实验不提供Windows长驻、可见FPS或完整Mod组合的统一速度结论；不将不同基线、不同节点预算、局部和整搜百分比相加。高成本方向保留证据与适用条件，不把未采用原型计为生产优化。
